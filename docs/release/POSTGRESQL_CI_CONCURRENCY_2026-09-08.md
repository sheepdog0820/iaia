# PostgreSQL CIの並行処理テスト補完（2026-09-08）

## 変更と理由

SQLiteのUnit/Integrationジョブで省略される25件を照合し、既存の並行処理テスト11件がPostgreSQLジョブの対象に含まれていないことを確認した。対象の6モジュールを追加し、既存の対象は維持した。アプリの仕様やDBスキーマは変更していない。

| SQLiteで省略される対象 | 件数 | PostgreSQL CIの対応 |
| --- | ---: | --- |
| Google grant / identityの並行処理 | 3 | 今回追加 |
| グループ招待の並行処理 | 3 | 今回追加 |
| OAuthの原子性・並行処理 | 2 | 今回追加 |
| 参加枠のclaim・報酬の並行処理 | 3 | 今回追加 |
| 日程調整の確定・作成の並行処理 | 5 | 既存対象 |
| リリース前DB確認 | 9 | 既存対象 |

追加モジュールには並行処理以外の3件もあり、PostgreSQLジョブ全体は249件から263件になった。

## 検証

- 変更前のアプリコミット `67221be68ad338bbfe5e82ddce8bebfc584eb09b` の[CI実行](https://github.com/sheepdog0820/iaia/actions/runs/34174163214)は全5ジョブ成功。Unit/Integrationは1,713件成功・25件省略、PostgreSQLは249件成功、Playwrightは186件成功。
- ローカルの隔離PostgreSQL 16で追加6モジュールを実行し、14件成功（65.194秒、省略なし）。
- コミット `2b34cbddbf84d17e20a1ec1c077a3556c25697ac` の全ソースを `git archive` で取り出し、変更後のワークフローから本番用設定とテストコマンドを読み込んで実行した。接続先は使い捨てPostgreSQLに差し替え、マイグレーションと静的ファイル収集後に **263件成功、26 subtests成功、9 warnings（115.83秒、省略なし）**。警告は既存マイグレーションのDjango非推奨APIに関するもの。
- 初回の準備はコンテナ内の展開先への書き込み権限で失敗した。テスト実行前の失敗であり、書き込み可能な `/tmp/validation` に修正して上記検証を完了した。
- YAMLは検証ランナーで解析し、追加後のジョブコマンドそのものを実行した。差分の自己レビューで既存対象の維持、対象ファイル、外部接続を行わない検証設定を確認した。

ローカル証跡は `tmp/ci-concurrency-production-retry.log`、実行補助は `tmp/run-ci-concurrency.py`。これらはGit管理対象外。Google/OAuth試験はテスト用応答による検証であり、実アカウントの外部連携確認を代替しない。共有環境・本番DB・権限・課金設定への変更はない。

## 残る確認

変更後コミットのGitHub CI完了は別途確認する。正式公開の可否は、実サービス連携・運用復旧・セキュリティ残課題などを含む受け入れ条件で判断する。この変更だけで正式公開可能とはしない。取り消す場合は、このCI変更のコミットをrevertする。DBの復旧操作は不要。
