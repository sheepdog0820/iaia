# Stripe連携の再開タスク

更新日: 2026-09-19。Stripe登録・追加認証とサンドボックスへのCLI接続が完了。隔離環境で実Checkout、署名付きWebhook、更新請求、支払い失敗・回復、期間末解約、通知順序逆転、契約を残した利用者削除の防止を検証した。実装候補は `6ff9a1f9`。開発AWSへの接続・反映や残る実環境確認は未完了であり、正式公開の必須条件B01〜B05は未完了。

## 現在地

- ビジネス名「タブレノ」、所在地「日本」、承認済みの事業内容を登録した。Bitwardenの設定はユーザーが担当する。
- Chromeで「タブレノサンドボックス」とテスト環境の表示を確認した。本番環境の有効化はユーザー申告であり、本番決済は実行していない。
- 同一商品に月額480円・年額4,800円のJPY継続料金を作成し、保存後の一覧で確認した。詳細は [今回の検証記録](STRIPE_SANDBOX_VERIFICATION_2026-09-13.md) を参照。
- Stripeコネクターは再認証が必要だが、別プロファイル `tableno-sandbox` のCLI接続は完了。アカウントID `acct_1UFA5iE2s31mT3or` が一致し、テストキーのみ利用可能、本番キーなしと確認した。既存defaultプロファイルは変更していない。
- 実課金しないテスト環境での検証は承認済み。本番課金開始の承認ではない。

## 接続状態

本人の追加認証と明示的なアクセス許可を受け、CLIの接続完了を確認した。テストキーの表示上の期限は2026-12-12。秘密鍵は既存CLIの認証ストアからプロセス内だけで読み取り、文書・Git・コマンド引数には保存していない。Webhook転送プロセスと隔離ローカルサーバーは検証終了後に停止済み。

## Codexが再開する作業

1. 候補6ff9a1f9の[CI全6ジョブ成功](STRIPE_CI_6FF9A1F9_2026-09-19.json)を確認済み。[反映案](STRIPE_AWS_APP_APPROVAL_2026-09-19.md)に基づくmainマージ、共有DBの0065〜0067適用、開発AWSのアプリ反映は対象を明示した承認後に行う。
2. 開発AWSのStripe接続を整備する。既存テストキーは読み取り検査で認証エラーとなり、CLIで接続済みのサンドボックスと同一アカウントであることを確認できていない。長期利用するサービス用テストキー、Price、Webhook署名鍵の具体的な設定案を用意する。期限付きCLIキーをサービス用として転用しない。
3. 課金メールの常設配送・再試行と監視を整備する。配送待ちDBと再試行処理は実装済みだが、AWSにworker/beatがない。Redis等を含む案は別途継続費用の承認が必要。SMTP認証・実配送・障害時復旧は未確認で、メール送信先の承認も必要。
4. 開発AWSで、ログイン状態を維持した購入→復帰→プレミアム反映、Portal、Webhook再送、解約・失効を確認する。ローカルのStripe画面・API検証をAWSの一連の動作確認として扱わない。
5. 公開用問い合わせ先での受信・返信、請求開示の運用、監視・復旧・販売開始条件を確認し、[正式公開受入表](FORMAL_RELEASE_ACCEPTANCE_MATRIX.md)のB01〜B05を更新する。

## 完了した追加検証

- 年額Checkout、3D Secure、Portalの支払い方法更新を含む[追加検証](STRIPE_ADDITIONAL_VERIFICATION_2026-09-13.md)。
- [Checkout再試行・二重作成防止](STRIPE_CHECKOUT_RETRY_2026-09-13.md)、[古いCheckout完了通知](STRIPE_CHECKOUT_SIGNED_ORDER_2026-09-19.md)、[請求順序逆転](STRIPE_INVOICE_ORDERING_2026-09-19.md)、[異議申し立て順序逆転](STRIPE_DISPUTE_ORDERING_2026-09-19.md)、[複数の利用停止理由](STRIPE_MULTIPLE_REVOCATIONS_2026-09-19.md)を検証した。個別記録の試験条件を超える網羅性は主張しない。
- 利用者削除の保護を退会画面・通常API・管理者API・管理画面まで統一。[実Stripe接続23項目](STRIPE_DELETION_PATHS_API_2026-09-19.md)、[署名付き通知と退会の両ロック順](STRIPE_DELETION_RACE_2026-09-19.md)、[配布イメージ内の回帰テスト](STRIPE_CONTAINER_6FF9A1F9_2026-09-19.md)が成功した。任意の管理スクリプトや直接SQLまで防ぐDB制約ではない。

共有環境の設定変更など、既存承認の範囲を超える操作は具体案を用意して確認する。定期実行・通知は設定していない。販売開始・本番課金は今回の再開指示に含めない。

## 再開時の実装確認

main/開発AWSのコードd875d028の通常配布イメージで、ネットワークなし・使い捨てメモリSQLite・隔離設定により `accounts.test_billing`、`tests.unit.test_billing_legal_pages`、`tests.unit.test_production_settings` の229テストが成功（17.258秒）。ログはGit管理外の `tmp/stripe-resume-baseline-20260913.log`。期待する障害応答を模擬したERRORログを含むが、テストの失敗はない。実StripeイベントやCheckoutを検証した証拠にはしない。

料金は月額480円・年額4,800円（税込）、有料機能は背景透過のみ。Checkout/Customer Portal/Webhookと監査ログの既存実装を基盤に、登録後の実テストで不足を修正する。SDKクライアント方式、再試行・競合・重複契約の防止もレビュー対象。税の自動計算設定は、登録・適用条件を確認せず有効化しない。
