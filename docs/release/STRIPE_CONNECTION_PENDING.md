# Stripe連携の再開タスク

更新日: 2026-09-13。Stripe登録・追加認証とサンドボックスへのCLI接続が完了。隔離したローカルDBで、実Checkout、署名付きWebhook、更新請求、支払い失敗・回復、期間末解約を確認した。Customer Portalの解約予約を認識できない不具合を修正し、実画面操作で再検証済み。開発AWSへの接続・反映や残る異常系検証は未完了のため、正式公開の必須条件B01〜B05は未完了。

## 現在地

- ビジネス名「タブレノ」、所在地「日本」、承認済みの事業内容を登録した。Bitwardenの設定はユーザーが担当する。
- Chromeで「タブレノサンドボックス」とテスト環境の表示を確認した。本番環境の有効化はユーザー申告であり、本番決済は実行していない。
- 同一商品に月額480円・年額4,800円のJPY継続料金を作成し、保存後の一覧で確認した。詳細は [今回の検証記録](STRIPE_SANDBOX_VERIFICATION_2026-09-13.md) を参照。
- Stripeコネクターは再認証が必要だが、別プロファイル `tableno-sandbox` のCLI接続は完了。アカウントID `acct_1UFA5iE2s31mT3or` が一致し、テストキーのみ利用可能、本番キーなしと確認した。既存defaultプロファイルは変更していない。
- 実課金しないテスト環境での検証は承認済み。本番課金開始の承認ではない。

## 接続状態

本人の追加認証と明示的なアクセス許可を受け、CLIの接続完了を確認した。テストキーの表示上の期限は2026-12-12。秘密鍵は既存CLIの認証ストアからプロセス内だけで読み取り、文書・Git・コマンド引数には保存していない。Webhook転送プロセスと隔離ローカルサーバーは検証終了後に停止済み。

## Codexが再開する作業

1. [追加検証記録](STRIPE_ADDITIONAL_VERIFICATION_2026-09-13.md) の未完了項目を進める。年次更新・返金・異議申し立て勝敗・重複/失敗後再送・購読通知の順序逆転は実検証済み。
2. 請求や異議申し立てをまたぐ順序逆転、PostgreSQLでの並行処理、二重契約防止を確認・修正する。
3. 年額Checkout画面、3D Secure、Portalの支払い方法更新を検証する。
4. 開発AWSのテスト設定・Webhook・画面導線を整備して検証する。共有環境への秘密情報設定は具体的な変更案と承認範囲を確認する。
5. 通知メール・法務・販売開始条件の運用準備を確認し、B01〜B05の未完了条件を更新する。

共有環境の設定変更など、既存承認の範囲を超える操作は具体案を用意して確認する。定期実行・通知は設定していない。販売開始・本番課金は今回の再開指示に含めない。

## 再開時の実装確認

main/開発AWSのコードd875d028の通常配布イメージで、ネットワークなし・使い捨てメモリSQLite・隔離設定により `accounts.test_billing`、`tests.unit.test_billing_legal_pages`、`tests.unit.test_production_settings` の229テストが成功（17.258秒）。ログはGit管理外の `tmp/stripe-resume-baseline-20260913.log`。期待する障害応答を模擬したERRORログを含むが、テストの失敗はない。実StripeイベントやCheckoutを検証した証拠にはしない。

料金は月額480円・年額4,800円（税込）、有料機能は背景透過のみ。Checkout/Customer Portal/Webhookと監査ログの既存実装を基盤に、登録後の実テストで不足を修正する。SDKクライアント方式、再試行・競合・重複契約の防止もレビュー対象。税の自動計算設定は、登録・適用条件を確認せず有効化しない。
