# 日程調整の隔離環境性能試験

## 結果

2026-09-12、通常配布イメージa32b6a86で登録100人・同時10フローを測定した。日程調整の作成、参加者の回答、集計確認、確定、セッション日時の再取得を各100件、合計500要求実行。全件が期待したHTTP状態・保存内容に一致し、エラー0件だった。

日程確定のp95は3.489秒で、承認済みの3秒基準を超えた。他の4操作は今回の測定では基準内。性能条件Q02と正式公開は未達のまま。

| 操作 | 成功/要求 | p95秒 | 最大秒 |
| --- | --- | --- | --- |
| 候補3件の作成 | 100/100 | 2.507 | 2.692 |
| 参加者の回答 | 100/100 | 1.998 | 2.298 |
| 回答集計の取得 | 100/100 | 1.989 | 2.011 |
| 日程確定 | 100/100 | 3.489 | 3.600 |
| セッション日時の再取得 | 100/100 | 2.026 | 2.305 |

## 条件と検査範囲

- [検証済み通常イメージ](RUNTIME_CANDIDATE_A32B6A86_2026-09-12.md)を使用。アプリのソース差し替えなし。アプリ0.25 CPU/512 MiB、PostgreSQL18.3 2 CPU/1 GiB、Redis7 128 MiB。
- 新規の内部Dockerネットワーク・一時DBを使用。ホストへのポート公開、実データ、AWS、Googleへの送信なし。起動時のmigrate/collectstaticはこの使い捨て環境だけに適用。
- 利用者100人、各人が所有する非公開グループ100件と、キャラクター・シナリオ・セッション各1,000件を準備。各グループに別の利用者1人が参加し、グループの先頭セッション1件を日程未定にした。
- 各フローは所有者のTokenで候補を作成し、グループ参加者の別Tokenで1候補へ回答。所有者として集計のavailable_count=1を確認し、同じ候補で確定。応答の締切状態と元セッションID、再取得したセッションの日時を照合した。
- 100フローを最大10並列、人間の操作待ち時間なしで実行。各HTTP開始からJSON受信までを計測し、nearest-rankでp95を集計。1回の測定であり、TLS、実RDS、ブラウザ表示、候補数や参加人数が多い場合を含まない。
- 終了0は内容と件数の検査結果で、性能合格を意味しない。所有ラベルを確認して試験コンテナ3件とネットワークを削除し、残存なしを確認した。

## 次の検証

日程確定時のDB照会・シリアライズを計測し、原因を切り分ける。現時点ではロックや権限確認を削減していない。対策後は同じ条件で再測定し、応答内容・権限・同時確定の保護も再検証する。実AWSや通常操作全体への性能合格を拡大解釈しない。

Git管理外の証跡は `tmp/poll-performance-a32b6a86/`。`run.py`、`seed.py`、`write_probe.py`、`poll-results.json`、`writes.log`、`server.log`を保持。認証トークン・runtime.envは使い捨てのローカル試験専用で、Gitへ登録しない。

## 原因分析と照会削減

日程確定時にモデルのrefresh_from_dbで事前取得した関連データが消え、応答の作成中に候補ごとの回答者と投票数を再取得していた。通常イメージの隔離SQLiteでCaptureQueriesContext/cProfileを使い、候補3件で44照会、15件で104照会、そのうち集計COUNTは9/45照会と確認した。15候補ではシリアライズが計測処理の約0.102/0.143秒を占めた。この単発診断の秒数を負荷試験のp95とは比較しない。

確定後の表示用データをselect_related/prefetch_relatedでまとめて取得し、投票が事前取得済みの場合はその回答から件数を計算するよう修正。未取得の場合は従来のDB集計を維持する。ロック・権限確認・確定方針・応答項目は変更しない。修正後の同じ診断では候補3件/15件とも29照会、集計COUNTは0になった。

追加テストは修正前に2件失敗を再現。修正後はSQLiteで関連23件成功・6件省略、PostgreSQL18.3で権限・同時処理を含む関連34件成功。事前取得あり/なしの応答一致、投票数2/1/1、確定日時と回答一覧の保持も検査した。Black/isort/flake8、差分・応答互換性レビュー成功。新しい画面文言はない。

DBスキーマ・共有環境・Secrets・権限・費用の変更なし。戻す場合は当該コミットをrevertする。診断ログは `tmp/poll-confirm-profile-before.log` / `tmp/poll-confirm-profile-after.log`、PG結果は `tmp/poll-response-pg.log`。試験DB・ネットワークは削除済み。通常配布物での同条件再測定と修正候補CIは未完了のため、3秒基準の達成はまだ主張しない。

## b803d7adの再測定

修正コミットb803d7adをgit archiveから標準Dockerfileで再ビルドし、終了0。通常イメージ `tableno-release:b803d7ad` のIDは `sha256:f7437c6a56f600d8b342f028be77f12ceb8f3bc494e857294f111d8c550bc7ff`。前回と同じ資源制限・100人・同時10フローを、新規DBと同じ測定器で再実行した。ソース差し替えなし。

| 操作 | 成功/要求 | p95秒 | 最大秒 |
| --- | --- | --- | --- |
| 候補3件の作成 | 100/100 | 2.501 | 2.793 |
| 参加者の回答 | 100/100 | 2.412 | 2.509 |
| 回答集計の取得 | 100/100 | 1.998 | 2.209 |
| 日程確定 | 100/100 | 3.290 | 3.593 |
| セッション日時の再取得 | 100/100 | 2.096 | 2.293 |

全500件の内容一致・エラー0、試験コンテナ/ネットワーク削除確認済み。日程確定は3秒を超え、今回の照会削減だけでは性能基準を達成できなかった。前回3.489秒との数値差は各1回の結果で、安定した改善幅を証明しない。本番相当設定で処理時間の内訳をさらに計測する。Q02/正式公開は未達、修正候補CI・AWS反映は未確認。

証跡はGit管理外の `tmp/poll-performance-b803d7ad/` と `tmp/release-b803d7ad-build.log`。既存Web/DB・Google・権限・費用への変更はない。

## 本番相当設定での時間内訳と追加改善

b803d7adの通常イメージ、本番相当設定、同じ0.25 CPU/512 MiBとデータ量の新規隔離環境で、Token認証付きAPIClientによる日程確定5件をcProfile/CaptureQueriesContextで計測した。単発経過時間は0.201〜0.208秒、プロセスCPU時間は0.053〜0.058秒、29照会のクライアント側SQL計測は0.141〜0.150秒。SQL計測にはネットワークやCPU割当て待ちも含むため、DBサーバーの実行時間と同一視しない。同時HTTP試験のp95ではない。

最終標本ではget_locked_pollが約0.100秒を占め、確定前の可視性確認・ロック後の再確認で表示用の候補/回答/回答者まで取得していた。さらに確定後にも表示用取得を行うため、回答テーブルの読み取りは計3回だった。confirmアクションに限って事前の表示用prefetchを省き、確定後の1回だけにした。可視性フィルター・権限・ロック・再確認は維持し、他アクションの事前取得は変えていない。

追加した照会検査で修正前3回/期待1回の失敗を再現し、修正後はPostgreSQL18.3の関連34件成功（2.775秒）。取得済み/未取得集計の一致、確定日時・回答保持、候補数増加、同時確定と権限拒否を含む。書式・差分レビュー成功。新しい表示文言・DB移行なし。

計測証跡はGit管理外の `tmp/poll-runtime-profile-b803d7ad/profile-results.json`。隔離資源は削除済み。追加改善後の通常HTTP再測定・候補CI・AWS反映は未完了。

## 82712c24の通常配布物で基準内を確認

82712c24をgit archiveから標準Dockerfileでビルドし、終了0。通常イメージ `tableno-release:82712c24`、同じCPU/メモリ制限・100人・最大10フロー・新規DB・同じ測定器で再測定した。ソース差し替えなし。

| 操作 | 成功/要求 | p95秒 | 最大秒 |
| --- | --- | --- | --- |
| 候補3件の作成 | 100/100 | 2.507 | 2.706 |
| 参加者の回答 | 100/100 | 2.187 | 2.289 |
| 回答集計の取得 | 100/100 | 1.494 | 1.794 |
| 日程確定 | 100/100 | 2.227 | 2.685 |
| セッション日時の再取得 | 100/100 | 2.108 | 2.297 |

500件すべて内容一致・エラー0。今回の日程調整5操作はp95が3秒以内であり、先行2候補での当該フローの未達は、この再測定では解消した。1回の測定で、負荷時間・データ形状・実AWS・主要操作全体に広げた合格は主張しない。所有ラベルを照合して試験コンテナ/ネットワークを削除し、残存なしを確認。

証跡はGit管理外の `tmp/poll-performance-82712c24/` と `tmp/release-82712c24-build.log`。候補CI・AWS反映・Q02全体・正式公開は未完了。既存共有環境とGoogleには変更なし。

## 3候補すべてへの回答を追加検証

同じ通常イメージ82712c24、資源制限、登録100人・同時10フローで、各候補にavailable/maybe/unavailableを回答する2経路を、新規DBからそれぞれ測定した。集計の各件数と回答状態が一致することを確認した。

一括API経路は3件のvotesを1要求で送信し、500要求すべて内容一致・エラー0。p95は作成2.306秒、一括回答2.608秒、集計1.493秒、確定2.285秒、日時再取得2.010秒で、今回の測定は基準内だった。ただし現行画面 `session_date_poll.html` は1候補ずつ送信するため、一括APIを画面の通信方式とは扱わない。

画面の通信方式に合わせた経路は、候補ごとに回答を3回送り、計700要求すべて内容一致・エラー0だった。

| 操作 | 成功/要求 | p95秒 | 最大秒 |
| --- | --- | --- | --- |
| 候補3件の作成 | 100/100 | 2.708 | 2.913 |
| 参加可能を回答 | 100/100 | 1.983 | 2.221 |
| 未定を回答 | 100/100 | 2.202 | 2.388 |
| 参加不可を回答 | 100/100 | 2.295 | 2.490 |
| 回答集計の取得 | 100/100 | 1.603 | 1.897 |
| 日程確定 | 100/100 | 2.685 | 2.890 |
| セッション日時の再取得 | 100/100 | 2.094 | 2.499 |

両試験の専用コンテナ・ネットワークは所有ラベルを照合して削除、残存なし。証跡はGit管理外の `tmp/poll-full-performance-82712c24/` と `tmp/poll-sequential-performance-82712c24/`。これはToken認証のHTTP測定で、実ブラウザの描画・操作時間や実AWSを含まない。前段の単一候補回答も含めた日程調整の確認範囲は広がったが、候補/参加者が多い場合、長時間負荷、他機能の性能条件は残る。

## 候補CIの確認

アプリ82712c24を含む文書コミット6b70d517の[PR CI](https://github.com/sheepdog0820/iaia/actions/runs/34689604387)は6ジョブすべて成功した。一方、同コミットの[push CI](https://github.com/sheepdog0820/iaia/actions/runs/34689603089)は5ジョブ成功、playwright失敗だったため、両CIが安定して成功したとは扱わない。

push側ではFirefoxのグループ招待テストで、招待相手の新規登録画面への `page.goto('/signup/')` が30秒のテスト制限に達した。再試行は成功し、結果は185 passed / 1 flaky。CIの `failOnFlakyTests` により不合格となった。失敗時の画像には登録フォームが表示され、トレース内の当該ページと静的資源は200だったが、load待ちが終了しなかった原因はまだ確定していない。待機条件を弱める修正や制限時間の延長は行っていない。

失敗証跡はGit管理外の `tmp/ci-34689603089-browser.zip` と `tmp/invitation-trace-summary.log` に保存。Firefoxでの再現・待機条件の検証を後続タスクに残す。今回の追記は性能測定とCI結果の記録のみで、CI失敗を修正済みとするものではない。mainマージ・AWS反映・正式公開は実施していない。
