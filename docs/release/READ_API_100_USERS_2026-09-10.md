# 登録100人・同時10件の一覧API反復測定

## 判定

合意済みのp95 3秒以内に対し、今回の隔離ローカル構成では未達。セッション一覧は3回とも超過、シナリオ一覧は3回目に超過した。測定900件の予期しないエラーは0件だが、正式公開の性能合格とはしない。

| API | 1回目p95秒 | 2回目p95秒 | 3回目p95秒 |
| --- | ---: | ---: | ---: |
| キャラクター一覧 | 1.294 | 2.601 | 2.702 |
| シナリオ一覧 | 1.589 | 2.880 | 3.400 |
| セッション一覧 | 6.691 | 6.209 | 3.706 |

各回・各API100リクエスト。全900件HTTP200。別枠のウォームアップは同時1件、全100ユーザーを使用し、3 API計300件がHTTP200だった。p95はエラーも含めたnearest-rank方式で、クライアント側のHTTP応答完了までを測定した。

## 条件

- 現在の開発Webと同じ配布物 `aws-pre-fa09731c`。ローカルimage ID `sha256:3bfb18a2539b470964c4726422702e0b68d60e503d015f8cf30edb1962337ab4`。後続dd0fe128はテスト・文書の差分で、アプリコードは同一。
- アプリ0.25 CPU/512 MiB、PostgreSQL 18.3は2 CPU/1 GiB、Redis 7は128 MiB。Docker内部ネットワークのみ、空の一時DBと合成データを使用。通常entrypointで移行・静的収集・Daphne起動を実行した。
- 登録100人、各人キャラクター10件（6版・7版各5）、各人セッション10件・シナリオ10件。計キャラクター1,000、セッション1,000、シナリオ1,000、グループ100。各グループは作成者と次の利用者が所属する。
- 先頭ユーザーの認証付きGETでキャラクター10件、セッション20件、シナリオ30件が返ることを別途確認した。
- `tests/performance/read_load.py` をアプリと別のCPU制限なしコンテナから実行。対象は `/api/accounts/character-sheets/`、`/api/scenarios/scenarios/`、`/api/schedules/sessions/?period=all`。3種類を混在させ、全体の同時リクエスト上限10、100ユーザーのトークンを順番に使用。各API同時10件ずつではない。待ち時間を挟む人間10人の行動モデルとも異なる。
- 各回300リクエスト、タイムアウト10秒、3回を順番に測定。専用アプリ・DB・Redis・ネットワークは所有ラベルを照合して削除し、残存なしを確認。AWS容量・共有DB・権限・継続費用の変更なし。

## 制約と次の作業

TLS・実RDS・実S3・画像一覧・ブラウザ描画・保存/編集操作・長時間負荷は含まない。同時10件の混在GETであり、登録100人・同時10人の全利用フローが検証済みという意味ではない。ローカルホスト上のスケジューリングも影響し得るため、今回の結果だけでAWSの必要CPUや費用を決定しない。

次はセッション一覧とシナリオ一覧の処理時間を、DB待ち・レスポンス構築・CPU制限に分けて調べる。レスポンス項目や閲覧権限を削って測定値を合わせず、同じ代表データと応答を維持して改善後を比較する。保存・編集を含む負荷試験も別途必要。

証跡はGit管理外の `tmp/performance-100-fa09731c/`（run.py、seed.py、check.py、seed.log、fixture-requests.log、warmup.log、concurrency-10-a/b/c.log、server.log）。使い捨て認証情報のenvは公開・コミットしない。

## CI

フォントHTTP後処理の修正dd0fe128について、CI run34306247763は2026-09-10のAPI確認でUnit / Integration・system・production-database・playwright・lint-securityの全5ジョブsuccess。前回のフォントテスト失敗は解消している。CI成功と今回の性能未達は独立して記録する。
