# 日程調整の返信入力の準備確認（2026-09-08）

3da61949の[CI](https://github.com/sheepdog0820/iaia/actions/runs/34175817824)は、Firefoxの日程調整フローがタイムアウトし、再試行成功の1 flaky・185成功でブラウザジョブが失敗した。他の4ジョブは成功。再試行成功を全体合格にはしない。

## 証拠と修正

アーティファクト10037471357のトレースを確認した。GM画面をDOMContentLoadedまで再読み込みした直後、コメント入力欄は一時的に有効だった。fill中に入力欄が無効になり、fill後の値は空、送信クリック時にも空だった。初期コメント取得前の操作と初期化が重なっていた。コメント送信処理は空文字を送らないため、テストのPOST待機がタイムアウトした。

再読み込みと同時にコメントGETの待機を登録し、200応答と入力欄の有効化を確認してから入力するようにした。入力値の保持も確認してから送信する。固定待機時間の延長や再試行の追加は行っていない。アプリの振る舞いを修正した証拠ではなく、今回のCI失敗に対するテストの準備待ちの修正である。

## 検証

c97571f6のGitアーカイブ全体に変更後のテストを重ね、使い捨てSQLiteと既存Playwright 1.63イメージで実行した。Chromium・Firefox・WebKit各5回、合計 **15件成功（3.1分）**。再試行設定は0。コメント送受信・投票・確定・日時保存までの既存の検証を維持した。差分・文字コード・テスト手順を自己レビューし、追加の指摘なし。製品UI文言の変更はない。

ローカル証跡: `tmp/date-poll-ci-3da.zip`、`tmp/date-poll-failed-trace.zip`、`tmp/inspect-poll-snapshots.py`、`tmp/poll-readiness-full-run.sh`、`tmp/poll-readiness-validation.log`、`tmp/poll-readiness-validation-output/results.json`。トレースの認証情報は公開資料に転記しない。

専用コンテナは正常終了し削除済み。共有環境・実データ・DBスキーマ・Secrets・費用は変更していない。変更後コミットの全体CIは別途確認する。取り消す場合はこのテスト変更をrevertでき、DB復旧は不要。
