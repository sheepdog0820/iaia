# キャラクター画像操作の性能

## 判定

通常配布物cc160e7b、0.25 CPU/512 MiBで、約1.1MBのJPEGは対象5操作のp95が3秒以内だった。一方、許可範囲内の約4.5MBでは画像追加7.098秒・一覧3.485秒で未達。両試験500要求ずつはすべて期待結果に一致し、予期しないエラー0。軽い画像だけの成功を画像機能全体の合格に拡張しない。

## 条件

- 登録100人・同時10フロー、6版/7版を各50人。各人は他者と独立した非公開キャラクターを使用。
- 非公開グループ100、キャラクター/シナリオ/セッション各1,000の新規データ。各グループには所有者と別ユーザーが参加し、利用者の総数は100人。
- 所有者による画像追加、一覧、別ユーザーの一覧拒否、所有者による削除、空一覧確認を順に実行。各操作100件、版ごとに250件。
- Token認証のHTTP開始から本文の読み取りまでを測定し、nearest-rank法のp95を使用。人間の操作間隔は挿入しない。合成JPEGの生成時間は測定に含めない。
- 標準Dockerイメージ `tableno-release:cc160e7b`、本番設定、ファイル保存はローカル。内部専用Dockerネットワーク、PG18.3（2 CPU/1 GiB）、Redis7（128 MiB）。DB/Redisは使い捨てtmpfs、実AWS・S3・外部サービスへ接続しない。
- ソース差し替えなし。各試験は新規DBから開始し、専用資源の所有ラベルを照合して終了後削除。継続費用の変更なし。

## 0.25 CPU/512 MiBの結果

| 操作 | 1024×1536、1,138,638 bytesのp95秒 | 2048×3072、4,553,465 bytesのp95秒 |
| --- | --- | --- |
| 画像追加 | 2.931 | 7.098 |
| 画像一覧 | 1.495 | 3.485 |
| 他者の一覧拒否（期待404） | 1.406 | 2.298 |
| 画像削除 | 1.521 | 2.308 |
| 空一覧確認 | 1.306 | 2.005 |

画像追加は201・先頭順序0・メイン画像フラグ・画像URLの存在、一覧は保存したIDだけが1件、削除は204・空本文・再取得0件を検査した。画像ファイル自体のダウンロード・デコード・ハッシュ一致や、既存画像4枚への5枚目追加は、この測定の検証範囲に含めていない。

## 証跡と残課題

### 0.5 CPU/1 GiBとの比較

同じcc160e7b、2048×3072・4,553,465 bytesのJPEG、登録100人・同時10フローを新規DBで測定し、500要求すべて期待結果に一致、エラー0。p95は画像追加3.652秒、一覧1.278秒、他者一覧拒否1.189秒、削除1.285秒、空一覧確認0.982秒。画像追加は改善したが3秒基準には未達であり、この資源構成を採用すれば合格とは言えない。

CPUとメモリを同時に増やしたため、改善をCPUだけの効果とは断定しない。CPU制限の計測ファイル `/sys/fs/cgroup/cpu.stat` が存在せず、最初の試行は画像操作前に中止・資源削除した。その環境依存の計測だけを外した新規試行で上記結果を取得したため、CPUスロットリングやピークメモリは未測定。証跡は `tmp/large-image-halfcpu-r2-cc160e7b/`、中止記録は `tmp/large-image-halfcpu-cc160e7b/`。いずれも専用資源は削除済み。

Git管理外の `tmp/image-performance-cc160e7b/image-results.json` と `tmp/large-image-performance-cc160e7b/image-results.json` に全要求の時間・版・成否、同ディレクトリに試験スクリプトとログを保存。アプリの起動・種データ作成は集計外。

画像追加のソース確認では再圧縮・縮小処理は見当たらないが、これだけでCPU不足や遅延箇所を断定しない。資源量の比較と実行時プロファイル、実S3、既存画像あり、長時間負荷、実ブラウザの送信・描画時間を含む確認が残る。画像サイズ制限や機能を変更して合格扱いにはしない。

## 処理段階の切り分け

通常イメージ内のAPIClientで、初期化後に同じ大きな画像を6版/7版各2回追加した。経過時間は0.097〜0.290秒、CPU時間は0.028〜0.071秒。初回に取得した別のプロファイルはクライアント初期化やWhiteNoiseの一覧構築を含んでいたため、通常要求の処理内訳として採用しない。APIClientはHTTPサーバー経由でも同時実行でもなく、基準合格の証拠ではない。証跡は `tmp/image-warm-profile-cc160e7b/`。

続いて試験専用のDaphne/ASGI計測を読み込んで0.25 CPU/512 MiB・100人/同時10フローを測定。500要求が期待結果に一致しエラー0、追加p95は7.425秒。追加100件の本文受信p95は3.803秒、ASGIの本文読み取り0.004秒、ASGI開始から応答開始3.808秒。各段階のp95は別々の分布であり、足し合わせて総p95とはしない。受信中のファイル書き込みCPUはp95約0.005秒だが、サーバー受信処理全体のCPUを測った値ではない。証跡は `tmp/image-http-profile-cc160e7b/`。この計測付き構成も通常配布物の性能合格としては扱わない。

## 1 CPU/2 GiBの通常配布物

試験専用の計測を外し、通常cc160e7bイメージを1 CPU/2 GiBで実行。2048×3072・4,553,465 bytesのJPEG、登録100人・同時10フロー、新規DBという条件で500要求が期待結果に一致し、エラー0だった。

| 操作 | p95秒 | 最大秒 |
| --- | --- | --- |
| 画像追加 | 1.941 | 1.960 |
| 画像一覧 | 0.698 | 0.801 |
| 他者の一覧拒否 | 0.637 | 0.788 |
| 画像削除 | 0.550 | 0.792 |
| 空一覧確認 | 0.630 | 0.800 |

この測定では全5操作が3秒以内だった。証跡は `tmp/large-image-onecpu-cc160e7b/`。各計測の専用コンテナ・ネットワークは削除済み。CPUとメモリを同時に変えた単発比較であり、CPUだけが原因、全画像操作が合格、実AWSでも同じ速度になるとは断定しない。アプリの検証・権限・画質・サイズ制限は変更していない。

## 増強案の費用と承認境界

9月12日にAWSの[東京リージョン価格JSON](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonECS/current/ap-northeast-1/index.json)を取得。公開日時2026-09-11T12:44:25Z、有効日2026-07-01のLinux/x86 Fargate通常価格はvCPU時間0.05056米ドル、GiB時間0.00553米ドル。SKUはKBQ3Q6DY9J327G8N/JQEE6EF5FAF2AESH。証跡は `tmp/fargate-image-capacity-prices-20260912.json`。

現行タスク定義48はCPU256・メモリ512。Webタスク1台を1 vCPU/2 GiBへ変更する場合の計算資源の比較は次のとおり。

| 条件 | 現行0.25 vCPU/0.5 GiB | 案1 vCPU/2 GiB | 増分 |
| --- | --- | --- | --- |
| 1時間 | $0.015405 | $0.061620 | $0.046215 |
| 月730時間 | 約$11.25 | 約$44.98 | 約$33.74 |
| 月360時間 | 約$5.55 | 約$22.18 | 約$16.64 |

1 vCPU/2 GiBは[公式の対応構成](https://aws.amazon.com/fargate/pricing/)に含まれる。上表はオンデマンドのWeb計算資源のみで、税、為替、RDS、S3、CloudFront、ログ、IPv4、worker等を含まない。月360時間は稼働時間の仮定であり、夜間停止を設定したという意味ではない。予約割引等は見込んでいない。

増強は検証候補として提示する段階。継続費用が増えるため、従来の1米ドル以内の一時試験許可で実施しない。既存のmain/AWS反映承認依頼にも資源増強は含めていない。採用・実環境反映・実S3試験の承認後、対象範囲を照合して進める。現在のAWSは変更していない。
