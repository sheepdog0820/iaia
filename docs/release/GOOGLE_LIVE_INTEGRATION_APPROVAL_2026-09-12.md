# Google実サービス検証の承認案

状態: 未承認・未実施。前回のAWS接続試験はGoogle書き込み・業務データ変更を含まない。今回の実データ変更・連携設定変更は別に承認を受ける。

## 検証対象と事前証拠

固定コード `a32b6a866702a1ec0fb9ed1ec714d66574aa0692`、通常イメージ `tableno-release:a32b6a86`、ローカルイメージID `sha256:c617a83ec0632db84248ea6ea33351bb92872b5c3c78e445b3d67c110ee542d2` を使う。[検証記録](RUNTIME_CANDIDATE_A32B6A86_2026-09-12.md)のとおりpush/PRの全6ジョブ、通常配布物280テスト、ソース516ファイル一致を確認済み。稼働f97c7809からDBマイグレーション差分なし。

承認後、このイメージを既存ECRリポジトリの専用試験タグへ送信し、返されたregistry digestを記録してタスクを固定する。ローカルイメージIDをregistry digestと混同しない。別のコード・依存更新を混ぜない。既存Web定義48やWebのbroker設定、main、本番は変更しない。

OSスキャン41件（HIGH 1を含む）は未解決。限定試験でもこのリスクを保持し、試験成功を公開合格にしない。

## 変更する対象と上限

| 対象 | 操作と上限 |
| --- | --- |
| AWS | 東京・083773015316の既存開発VPC。一時Redis 7.1/cache.t4g.micro 1台、専用SG/subnet group、worker 1タスク、実行・後片付け用短時間タスク。作成開始から最大4時間で停止・削除へ移る |
| 通信 | Redisは転送時/保存時暗号化、非公開。TCP6379は既存ECS SGからのみ許可。既存IAM/SGの権限は拡大しない |
| 開発DB | 専用非公開グループ1件、将来日付のセッション1件、画像なし試験キャラクター最大2件、対応する同期記録・ジョブ最大10件を作成/更新/削除 |
| Google Calendar | 指定済み連携アカウントのprimaryに試験予定1件だけ作成・更新・取消。招待先を付けない |
| Google Sheets | 指定済みChromeアカウントで非共有の専用スプレッドシート1件を作成し、Characters!A:Qに試験キャラクターのみ出力。終了後、そのファイルだけ削除 |
| 連携設定 | 対象利用者だけのCalendar/Sheets ON/OFFを一時変更し、元に戻す。期限切れ時の通常の認可更新・保存を含む。追加scope、意図的な認可取消、別アカウント作成は含めない |

ログイン利用者とGoogle連携先を開始前に照合する。異なるアカウント・複数候補・scope不足なら作成前に停止。トークンをローカル・会話・ログへ取り出さない。識別名 `tableno-live-google-YYYYMMDD-HHMM` と作成ID・所有者を記録し、既存の予定・シート・キャラクターを試験対象に含めない。

## 実行と合格条件

1. 現在のAWS正常性・夜間停止時刻を読み取り、後片付けまでの時間を確保する。0.25 vCPU/512 MiB、concurrency 1のworkerを専用Redis・専用キューで起動。beatは起動せず、RUN_MIGRATIONS/RUN_COLLECTSTATIC/CREATE_DEV_LOGIN_USERをfalseにする。DBセッション方式を維持する。
2. TLS検証付きPINGとworker応答を確認。連携OFFで専用データを作成し、宛先シートが空・非共有であることを確認する。
3. 連携ONにして、対象IDを明示した専用ジョブを投入する。Calendar作成・日時/タイトル変更・同じ対象への再実行で予定が1件、IDが同じであることを照合する。
4. Sheetsに指定キャラクターだけが出力されることを確認。後から別の試験キャラクターを追加しても、元ジョブの再実行が出力対象を拡大しないことを確認する。未作成タブへの出力で失敗させ、同じ専用ファイル内にタブを作成して再試行する。
5. 連携OFFでジョブが拒否され外部更新が増えないことを確認。通常の認可更新が自然に発生した場合は保存と継続成功を観測し、未発生なら未検証と記録する。
6. Calendar連携を試験用にONへ戻し、試験予定を取消してGoogle側とアプリ状態を照合する。通信切断による応答喪失は意図的に発生させず、模擬試験と区別する。

失敗、対象不一致、実データ混入、権限拡大の必要、費用超過見込みでは後続試験を止める。Web画面全経路、常設worker/beat、意図的な認可失効、Google公開審査、同時負荷は別の残条件。

## 後片付けと復旧

- 作成ID・試験名・所有者を照合して、試験予定と試験シート1件だけ削除。シートはそのファイルだけをゴミ箱から完全削除し、ゴミ箱全体は空にしない。Chrome接続が失われた場合は残存を報告する。
- workerのジョブ終了後に停止し、試験ジョブ・同期・キャラクター・セッション・グループの記録IDだけ削除する。DB全体の復元や既存データの整理はしない。
- 対象利用者の連携設定を元に戻す。ユーザーの別変更と競合した場合は上書きしない。正常更新された認可トークンは古い値へ戻さない。
- 一時タスク・Redis・subnet group・専用SGを削除し、試験タスク定義を登録解除。専用ECRタグを削除する。既存イメージ/定義には触れない。残存照会とWeb/DB/cache正常性で終了を確認する。
- AWS/Google障害等で削除できなければ、対象・時刻・費用見込みを報告して残存を追跡する。4時間や1米ドルは強制課金上限ではない。

## 費用

2026-09-12にAWS公開価格表を再取得した。東京Linux/x86 FargateはvCPU $0.05056/時、メモリ $0.00553/GiB時、Redis cache.t4g.microは$0.025/時。[東京ECS価格表](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonECS/current/ap-northeast-1/index.json)、[東京ElastiCache価格表](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonElastiCache/current/ap-northeast-1/index.json)。公開IPv4は[料金表](https://aws.amazon.com/vpc/pricing/)の$0.005/時。

4時間でRedis約$0.10、worker約$0.0617、IPv4約$0.02。短時間タスク・一時ECR保存・少量のログ/通信等を含め概算$0.20〜$0.30、1米ドルを超える見込みなら停止・削除を優先する。実請求額ではなく、税等も別。継続サービスや契約追加は含めない。

## 承認対象

上記範囲の一時AWSリソース・専用SGの作成削除、専用試験データとGoogle予定/シートへの限定書き込み・後片付け、対象利用者の連携ON/OFF・通常認可更新を承認対象とする。メール/他利用者への正式通知、本番反映、既存Webの更新、追加scope、恒久費用の増加は別の承認対象。
