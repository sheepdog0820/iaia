# 日程調整の二重確定と失敗時の復旧

2026-09-08、作業ブランチ272ed4bfからの変更。受け入れ条件F03の競合を調査し、日程確定の2件の不具合を再現・修正した。

## 再現と修正

確定APIは読み取り時点のis_closedだけを検査していた。先行要求が確定した後も、取得済みの古いオブジェクトを使う後続要求は200となり、別セッションを作成できた。また、確定済みフラグの保存とセッション作成が別々に行われ、セッション作成失敗後に投票だけが閉じたまま残った。

新規回帰テストで、古いオブジェクトを返す後続API要求と、セッション作成例外を再現。変更前はいずれも失敗した。実時間に依存する待機やテストの再試行は加えていない。

DatePoll.confirm_dateをトランザクション化し、投票行のロック取得後に締切状態を再確認する。既に閉じている要求は日本語の400応答とする。ロック保持中にDBの現在値を読み直し、日程保存・セッション作成/更新・関連付けをまとめて確定する。失敗時はDB変更全体を戻し、再試行できる状態を維持する。

## 検証

- 隔離SQLite: 新規テストは変更前2失敗/1スキップ。変更後は高度な日程調整・キャッシュ検査を含め24成功/1スキップ（21.341秒）。
- 隔離PostgreSQL 16: 同じ25件すべて成功（23.748秒）。2接続が異なる候補と同じ未確定投票を読み終えてから同時確定し、成功1件・締切拒否1件、セッション1件、投票とセッションの日付一致を確認した。lock_timeoutは5秒、同期待ちは10秒で制限。
- SQLiteのスキップは行ロックを要する並行試験であり、SQLiteの同時書き込み成功を証明しない。
- 既存セッションの日付・主開催日時更新、通常確定、再確定拒否、作成者以外の拒否を既存テストで確認。
- 変更したPython3ファイルのBlack/isort/Flake8と差分検査が成功。重要な日本語の締切メッセージは回帰テストで確認。
- CIのproduction-databaseジョブへ新規テストを追加。ローカル成功をリモートCI成功として扱わない。

テスト用イメージtableno-full:01a52f52に現行schedulesディレクトリだけを読み取り専用で重ね、ENV_FILE空・合成資格情報で実行した。PGは専用internalネットワークの空DBであり、共有DBや実ユーザーデータを使用していない。これは現行候補全体の再ビルド・全体検証ではなく、変更に対する対象検証である。

## 影響と残条件

スキーマ・Secrets・料金・外部サービス設定の変更はない。行ロックは確定処理の間だけ保持する。既存データに過去の二重セッションがあるかは調査しておらず、実データの修復も行わない。取り消す場合はこの変更をrevertできるが、二重確定と失敗時の不整合が再発する。

F03全体の完了ではない。後続の投票・候補追加・タイトルPATCHとの競合検証は次節を参照。異なる投票から同じセッションへの確定、明示的な日程再編集・再開、全権限変更との競合などは別の未確認条件として残る。

## af39eced後の追加: 他の書き込みとの競合

古い未確定オブジェクトを持つ投票・候補追加・タイトル編集を再現した。変更前は確定後の投票が200、候補追加が201となり、タイトルPATCHはis_closedをFalseへ戻し、セッションとの関連と確定日時も失い得た。閲覧できた後にグループを脱退したメンバーの投票も200で受理された。新規の4回帰テストはいずれも変更前に失敗した。

確定・投票・候補追加・通常更新を同じ投票行のロックで直列化し、ロック取得後にクエリセットの閲覧条件とオブジェクト権限を再確認する。更新シリアライザには最新のDBオブジェクトを渡す。これによりタイトルだけを変更する要求で、確定状態・日時・セッション関連を上書きしない。作成者以外の編集拒否メッセージを日本語化し、応答をテストで確認した。明示的な再開など、利用者が送った既存の編集フィールドの仕様は変更していない。

読み取り用クエリのDISTINCT・外部結合へFOR UPDATEを直接付けず、投票本体の主キーでロックする。待機後は閲覧条件を再評価するが、グループ会員行や共有設定も一括でロックするものではない。この再確認後に起きる全種類の権限失効まで原子的に防ぐという保証ではない。

- SQLite: 4件の不具合修正後、既存の日程調整・キャッシュ・グループ連携/ゲストを含め39成功/1スキップ（38.400秒）。追加の並行試験と日本語応答テストを含む最終対象ファイルは7成功/4スキップ（1.740秒）。
- PostgreSQL 16: 最終の関連44件すべて成功（57.341秒）。先行接続が投票行をロックし、別接続のAPI要求が未確定状態を読み取ったことをEventで確認してから、先行接続が確定・コミットする。投票と候補追加は400、タイトル編集は200で確定日・関連セッションを保持。既存の同時確定試験も成功。
- sleepや再試行でタイミングを合わせず、読み取りの観測とDBトランザクションで順序を制御。PostgreSQLのlock_timeoutは5秒。テスト追加済みファイルはproduction-database CIの実行対象。
- Black/isort/Flake8、差分・日本語応答のレビューに成功。現行schedulesのみを既存検証イメージへ重ねる対象検証であり、全候補のCI結果とは区別する。

共有環境は操作していない。専用PostgreSQLコンテナとinternalネットワークは所有ラベルを照合して削除した。DBスキーマ・Secrets・料金への変更はない。

## 3d1fef70後の追加: グループ移動と確定権限

作成時に存在する所属グループ検査が更新にはなく、作成者が未所属グループへ投票を移せた。関連セッションとは異なるグループへ移すこともできた。また、グループ管理者として投票を作成した利用者が一般メンバーになった後も、その投票からセッション日時を確定できた。既存の不整合グループを持つ投票も確定可能だった。新規5件のテストは変更前4失敗/1成功となった。

- 編集時に指定されたグループは、作成時と同じ「グループ作成者またはメンバー」の条件で検査する。関連セッションがある場合は、そのグループと一致することを必須とする。
- 確定時は投票作成者の確認に加え、関連セッションへの現在のcan_edit_session_basic権限とグループ一致を検査する。GMだけに限定せず、既存のセッション管理ロール・グループ管理者の仕様を維持する。
- 所属する別グループへの未関連投票の移動は引き続き成功。未所属グループへの移動を拒否した後、そのグループの作成者から投票を閲覧できないことも確認。
- 管理者降格後の確定は日本語403、グループ不一致は400となり、セッションの日付と投票の未確定状態を維持する。既存の権限を持つ作成者による確定は継続して成功。

関連49件はSQLiteで45成功/4スキップ（38.501秒）、PostgreSQL 16で49件すべて成功（56.668秒）。スキップ4件は行ロックを必要とする並行試験で、PGで成功している。変更したPython3ファイルのBlack/isort/Flake8と差分・日本語メッセージのレビューに成功。新規test_date_poll_edit_permissions.pyをproduction-database CIにも追加した。

検証は先の節と同じ既存イメージ＋現行schedulesの対象検証であり、共有環境は操作していない。専用PGとネットワークは所有ラベル確認後に削除した。スキーマや既存データの変更はない。過去の不正な移動や権限喪失後の確定が実データに存在するかは未調査であり、修復済みとはしない。すべてのロール変更と書き込みを単一トランザクションで直列化する保証でもない。
