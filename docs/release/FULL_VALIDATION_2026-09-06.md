# 2026-09-06 全体検証の記録

対象アプリのスナップショットは `c816f8e4`。後続の U2-Net モデル指定はこの全体実行には含まず、別途 [画像透過の検証記録](BACKGROUND_REMOVAL_MODEL_VALIDATION_2026-09-06.md) を参照する。

## バックエンド

実データと隔離した Docker 環境で accounts、api、scenarios、schedules、support、tableno、tests/unit、tests/integration を実行した。

| DB | 成功 | 失敗 | スキップ | カバレッジ |
| --- | ---: | ---: | ---: | ---: |
| SQLite | 1662 | 9 | 10 | 86.96% |
| PostgreSQL 16 | 1672 | 9 | 0 | 87.53% |

両方とも終了コード 1。元の全体実行を合格として扱わない。ローカル証跡は `tmp/full-c816f8e4-output/` の DB 別ログ、JUnit、カバレッジに保存した（Git 管理外）。

失敗は両 DB で一致した。

- CI の環境設定テスト 1 件：Playwright の `ENV_FILE` 未設定を要求していたが、独立した SQLite 検証のため明示的な空文字を設定済みだった。空文字と SQLite を検証するよう期待値を更新した。Compose 検証だけで `.env.example` を指定する検証は維持した。
- リポジトリ・文字品質検査 8 件（サブテストを含む）：Git archive の 847 ファイルは検証イメージと一致したが、`/candidate` に Git 管理情報がなく、Git コマンドが失敗した。アプリ機能の失敗とは区別する。

使い捨てコンテナ内で、生成物のないアーカイブの全ファイルを Git インデックスに登録し、修正したテストを読み取り専用で重ねて再検証した。対象は `test_docker_entrypoint.py`、`test_repository_hygiene.py`、`test_text_quality.py` の 3 ファイル。**32 件成功、4 サブテスト成功、警告 4 件、終了コード 0**。証跡は `tmp/full-fixture-regression.log`。ホストの Git 設定・インデックスには検証用の変更を加えていない。

この再検証は失敗箇所に限定しており、修正後の全体一括実行や GitHub CI の成功を示すものではない。実データ、DB スキーマ、アクセス権限、課金の変更はない。

## ブラウザとホームの HTML 挿入修正

同じ c816f8e4 の 111 ケースを Chromium・Firefox・WebKit、再試行なし、外部資産の取得可能な隔離環境で一括実行した。**106 成功・5 失敗、9.0 分、終了コード 1**。証跡は `tmp/browser-all-c816f8e4-final-output/results.json` と `tmp/browser-all-c816f8e4-final.log`。

2 件はホームで `/x` の画像取得が発生する失敗だった。グループ名・説明と履歴タイトルを未エスケープで HTML に挿入する箇所を確認し、DOM の `textContent` で文字をエスケープしてから表示するよう修正した。ほかの 3 件は WebKit のグループ一覧で対象カードを見つけられない失敗で、蓄積した公開グループによるページ分割にテストが対応していなかった。これらは別途修正・再検証する。

ホームの回帰テストは API 応答に画像タグとイベントハンドラーを含む文字列を注入し、修正前の Chromium で失敗を再現した。修正後は **3 ブラウザ 3 件成功、13.5 秒、再試行なし**。グループ名・説明・履歴タイトルの文字列一致、画像要素 0、イベントハンドラーの実行フラグなしを確認した。証跡は `tmp/home-security-red-verified.log`、`tmp/home-security-green.log`。API 応答を模擬した描画検証であり、全表示経路の安全性や修正後の全体一括合格を示すものではない。

## 失敗フローの再検証

招待承認後、所有者の権限操作、再読み込み後の参加者操作、非公開グループ作成後のテストを、画面のグループ名検索で対象カードを絞り込むよう修正した。作成後はモーダルが閉じることも確認する。カードや権限の検証は省略していない。

空の隔離 DB に開発用ユーザー 3 件と公開グループ 30 件を作成し、初めから一覧が複数ページになる条件で再実行した。対象は招待承認、管理者の付与・解除、非公開グループとセッション作成、主要画面の JS エラー検査、ホームの HTML 文字表示の 5 フロー。**3 ブラウザ計 15 件成功、2.1 分、再試行なし、終了コード 0**。証跡は `tmp/browser-regression.log` と `tmp/browser-regression-output/results.json`。

検証は c816f8e4 のブラウザイメージに、24233538 のホーム修正と更新したフローテストを読み取り専用で重ねて実施した。元の 111 ケース全体の失敗記録は保持し、15 件の再検証を全体一括成功に読み替えない。実環境への反映、実データ、課金、権限設定の変更は行っていない。自己レビューでは対象の検索・権限検証・日本語表示について追加の修正事項なし。

## ダッシュボード履歴の修正

6ee67201 後の監査で `templates/account/dashboard.html` に履歴タイトル・メモの未エスケープ挿入が残っていた。また、配列形式の履歴 API 応答を空の履歴として扱っていた。配列と results 形式の両方を受け取り、タイトル・メモを文字として表示するよう修正した。保存データや API の権限処理は変更していない。

模擬応答を使った Chromium の修正前テストは 2 件失敗。修正後は両形式の描画、実 API でのシナリオ・履歴作成から再読み込み後の表示、既存ホーム検査の **3 ブラウザ計 12 件が成功（23.4 秒、再試行なし）**。タイトル・メモの一致、画像要素 0、イベント実行フラグなし、保存済みメモの API 再取得を確認した。証跡は `tmp/dashboard-security-red.log`、`tmp/dashboard-security-green.log` と同名の `-output` ディレクトリ。

検証は c816f8e4 ブラウザイメージに対象テンプレートとテストを読み取り専用で重ね、空の隔離 SQLite を使った。実データ変更・本番反映なし。JavaScript 構文、差分、UTF-8、日本語表示を確認し、対象差分の自己レビューで追加指摘なし。全体 CI、外部連携、他の HTML 挿入経路の合格を意味しない。

## 共通の成功・エラーメッセージ

`static/js/arkham.js` の `showAlert` が渡されたメッセージを HTML として挿入していた。`textContent` を持つ span と閉じるボタンを DOM で作成する形へ変更した。閉じるボタンには日本語のアクセシブル名を付けた。既存の自動消去時間や表示位置は変更していない。

修正前の Chromium で失敗を再現し、修正後は **3 ブラウザ 3 件成功（19.6 秒、再試行なし）**。各ケース内で成功・エラー・API detail・API error の 4 経路を検証し、元の文字列の表示、画像要素 0、イベント実行フラグなし、閉じるボタンで消えることを確認した。証跡は `tmp/alert-security-red.log`、`tmp/alert-security-green.log` と対応する出力ディレクトリ。テストは共通 UI 関数に入力を渡すもので、実外部 API のエラー配送や全呼出元の検証を意味しない。

空の隔離 DB と c816f8e4 ブラウザイメージに対象 JS・テストを重ねて実行した。構文・文字品質・差分・日本語表示を確認し、自己レビューで対象差分の追加指摘なし。DB・秘密情報・費用への変更なし、本番未反映。GitHub CLI 認証はこの時点でも未設定であり、リモート CI の成功は未確認。

## セッション招待の文字表示

6899f690 後の監査で、セッション一覧の招待タイトル・招待者名に未エスケープ挿入を確認した。招待メッセージと同じ既存の `escapeHtml` に通して文字として表示するよう修正した。

修正前 Chromium の 1 件失敗を再現し、修正後は **3 ブラウザ 3 件成功（16.9 秒、再試行なし）**。模擬の招待 API 応答を使い、タイトル・招待者名・メッセージの一致、画像要素 0、イベント実行フラグなし、「承認して参加」「辞退」ボタンの表示を確認した。承認・辞退の API 操作や実配送はこのテストの対象外。証跡は `tmp/session-invite-red.log`、`tmp/session-invite-green.log` と対応する出力ディレクトリ。

6899f690 のブラウザイメージに対象テンプレートとテストを重ね、空の隔離 DB で実行した。JavaScript 構文確認は Django の `user.id` を検証用の数値に置き換えた抽出スクリプトで成功し、実レンダリングはブラウザで確認した。差分・UTF-8・日本語表示・自己レビューで追加指摘なし。実データ、DB、秘密情報、費用の変更なし、本番未反映。

## 固定候補の再検証環境と PR の制約

6899f690 の Git archive 全 852 ファイルとブラウザイメージのファイル内容を SHA-256 で全件照合し、一致した。イメージは `tableno-browser:6899f690`、ID は `sha256:46fb49b6efd89eaadb4fafc107640ea4a40382c92f24d3d462e38b9845cd433e`。ソースの上書きなしでブラウザ全体を実行する。

この全体実行は **125 件成功・1 件失敗、10.1 分、再試行なし、終了コード 1**。Firefox の秘匿ハンドアウトフローが 90 秒で停止した。画面は最初の参加者の公開グループ参加ボタンを表示した段階で、サーバーログにもグループ参加 POST やハンドアウト作成は記録されていない。後処理の context.close エラーが表示されており、元の停止原因を特定できていない。秘匿情報保護の成功にも漏えいの証拠にも読み替えない。証跡は `tmp/browser-all-6899f690.log` と `tmp/browser-all-6899f690-output/results.json`、失敗画面・動画。

同じ固定イメージで公開グループ 30 件を準備し、フローを 3 ブラウザ各 2 回、操作トレース付き・再試行なしで実行する診断を開始した。`tmp/handout-diagnostic.log` と `tmp/handout-diagnostic-output/` に保存する。この追記時点では診断結果待ち。

診断は **6 件すべて成功（1.8 分）**。各実行のトレースを保存したが、元の停止は再現せず、原因解消・全体合格とは扱わない。

元の全体実行では再試行を無効にしていた一方、Playwright の設定が `on-first-retry` だったため、初回失敗のトレースがなかった。`retain-on-failure` に変更し、成功時は破棄、初回を含む失敗時は保持する。[公式のトレース設定](https://playwright.dev/docs/test-use-options#trace-modes)を確認し、固定イメージの Playwright 1.53.1 でも動作検証した。

アプリや DB を使わない使い捨ての成功・意図的失敗各 1 件を再試行なしで実行した。設定変更前は trace.zip 0 件、変更後は失敗ケースだけに 1 件保存され、アーカイブ内に該当の失敗情報があることを確認した。両実行の終了コード 1 はこの検証入力による想定結果であり、アプリテスト失敗の無視ではない。入力テストはリポジトリに追加していない。証跡は `tmp/trace-retention-before.log`、`tmp/trace-retention-after.log` と各出力ディレクトリ。記録処理の負荷は増えるが、再試行回数・テストの合否条件は変えない。

バックエンドは a95e1a20 の全 853 ファイルが一致する `tableno-full:a95e1a20`（ID `sha256:0e90d02f6f7ec4e5bf51a2cbd1054a718d6f22ca071ee9a1e89156f3907f6d51`）を作成した。コンテナ内の非 root ユーザーがアーカイブから検証専用 Git インデックスを作成することで、前回の Git 管理情報不足を解消した。事前のリポジトリ・文字品質・CI 設定検査は 32 件・4 サブテスト成功。SQLite と PostgreSQL 16 の全体検証を開始したが、この追記時点では結果待ちであり合格扱いにしない。出力先は `tmp/full-a95e1a20-output/`。PG はポート公開なし・内部ネットワーク・tmpfs の使い捨て DB である。

GitHub 接続で対象ブランチの PR を検索し、該当なしを確認後、Draft PR 作成を再試行した。`403 Resource not accessible by integration` で拒否された。GitHub CLI は未認証、利用可能なアプリ内ブラウザも GitHub 未ログインだった。PR は作成できておらず、リモート CI の成功も未確認。通常の SSH push は成功している。権限・認証設定を変更せず、確認用ブラウザタブは閉じた。

## カレンダーのタイトル表示

カレンダーの次回セッションと既存セッション選択肢がタイトルを HTML に挿入していた。既存の `escapeHtml` を適用し、タグを含むタイトルも文字列のまま表示するよう修正した。

修正前 Chromium で 1 件失敗を再現し、修正後は **3 ブラウザ 3 件成功（21.2 秒、再試行なし）**。模擬 API 応答を使い、次回セッションのタイトル一致、画像要素 0、イベント実行なし、新規セッションモーダルから「既存セッションに日程を追加」への切替、選択肢のタイトル一致と選択を確認した。日程追加の保存はこの検査に含めない。証跡は `tmp/calendar-title-red.log`、`tmp/calendar-title-green.log` と対応する出力ディレクトリ。

6899f690 の固定イメージに対象テンプレートと検査を読み取り専用で重ね、隔離 DB で実行した。抽出 JavaScript の構文、差分、UTF-8、日本語表示を確認し、対象差分の自己レビューで追加指摘なし。保存データ・DB・秘密情報・費用変更なし、本番未反映。実行中の a95e1a20 バックエンド全体検証には、この後続のテンプレート修正は含まない。

## 参加者管理の名前・ゲスト・リンク表示

セッション一覧から開く参加者管理に、参加者名・キャラクター名の未エスケープ挿入と、ゲストの `user_detail=null` による表示停止を確認した。表示名、ニックネーム、ユーザー名、ゲストの順で安全な文字表示に統一した。キャラクターシートリンクは URL として解析できる HTTP/HTTPS のみ表示し、同じアプリ内の相対 URL も解決する。属性をエスケープし、別タブには `noopener noreferrer` を指定する。

最初のテストはアイコン付きボタンの名前完全一致で停止したため、その実行を中止して画面内の対象ボタンにセレクターを修正した。アプリ変更前の再検証では登録ユーザー名・ゲスト表示の 2 件が失敗した。証跡は `tmp/participant-display-red.log`（テスト側の停止）と `tmp/participant-display-red-verified.log`（アプリ側の再現）。

修正後は **3 ブラウザ 9 件成功（30.6 秒、再試行なし）**。模擬 API 応答から参加者ボタンをクリックし、名前・キャラクター名の一致、画像要素 0、イベント実行なし、HTTPS リンクの維持、javascript/data URL の非表示、壊れた URL・空 URL の非表示、名前未設定ゲスト・ユーザー名への代替表示、相対リンクの解決を確認した。初期の 6 件成功は `tmp/participant-display-green.log`、追加ケースを含む最終結果は `tmp/participant-display-final.log` と対応する出力ディレクトリ。

6899f690 イメージに対象テンプレート・テストを重ねた隔離検証であり、実データ保存や外部リンク先へのアクセスは行っていない。削除・参加者権限の API は変更していない。JavaScript 構文、差分、文字品質、日本語表示と自己レビューで対象差分の追加指摘なし。DB・秘密情報・費用変更なし、本番未反映。

## a95e1a20 バックエンド全体の完了結果

前述の結果待ちだった実行は、両 DB とも終了コード 0 で完了した。

| DB | 成功 | スキップ | 成功サブテスト | 警告 | カバレッジ | 時間 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| SQLite | 1669 | 10 | 480 | 159 | 86.96% | 1378.12 秒 |
| PostgreSQL 16 | 1679 | 0 | 480 | 159 | 87.54% | 1409.78 秒 |

JUnit は両方とも testcase 1679 件、failure/error 0。SQLite でスキップされた 10 件の classname/name を PostgreSQL の結果と照合し、すべて成功していることを確認した。アプリ 5 領域の集計カバレッジは閾値 70% を超えたが、全行・全分岐の検証や本番設定の合格を意味しない。実行範囲は accounts、api、scenarios、schedules、support、tableno、tests/unit、tests/integration。`APP_ENV=local` と選択した DB を使う隔離検証であり、GitHub CI、実 AWS、本番反映の証明ではない。

証跡は `tmp/full-a95e1a20-output/` の DB 別ログ、JUnit、XML/JSON カバレッジ。対象は a95e1a20 に固定され、その後のトレース設定・カレンダー・参加者管理の変更を含まない。c816f8e4 の旧全体失敗と 6899f690 のブラウザ全体失敗の記録は維持する。

2ec4ccc5 の全 855 ファイルが一致するブラウザイメージ `tableno-browser:2ec4ccc5`（ID `sha256:f4d70242537849658ae4dad411b2e9176b2332acb2600fd6ae224de4b784850d`）で、後続修正を含む 141 ケースの一括検証を開始した。再試行なし、初回失敗のトレース保持あり。この追記時点では実行中。証跡先は `tmp/browser-all-2ec4ccc5.log`、`tmp/browser-all-2ec4ccc5-output/`。

## セッション詳細の選択欄

日程参加者のチェック欄とキャラクター選択欄に、名前の未エスケープ挿入が残っていた。既存の `escapeHtml` を適用し、チェックボックスの値やキャラクター ID の扱いは維持した。

修正前 Chromium の 2 件失敗を確認し、修正後は **3 ブラウザ 6 件成功（33.2 秒、再試行なし）**。隔離 API でグループ・セッションを作成し、メンバー名・キャラクター名の取得応答を模擬した。日程追加ボタンからモーダルを開き、参加者名の一致、画像要素 0、イベント実行なし、チェックボックスの選択、キャラクター選択肢の名前一致を確認した。日程・キャラクター選択の保存はこの検査の対象外。

証跡は `tmp/session-choice-red.log`、`tmp/session-choice-green.log` と対応する出力ディレクトリ。2ec4ccc5 のイメージに対象テンプレートと検査を読み取り専用で重ねて実行した。実レンダリング、差分、UTF-8、日本語表示と対象差分の自己レビューで追加指摘なし。DB・秘密情報・費用変更なし、本番未反映。並行する 2ec4ccc5 の全体検証には、この後続修正を含めない。
