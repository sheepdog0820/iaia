# 2026-09-06 全体検証の記録

対象アプリのスナップショットは `c816f8e4`。後続の U2-Net モデル指定はこの全体実行には含まず、別途 [画像透過の検証記録](BACKGROUND_REMOVAL_MODEL_VALIDATION_2026-09-06.md) を参照する。

## バックエンド

実データと隔離した Docker 環境で accounts、api、scenarios、schedules、support、tableno、tests/unit、tests/integration を実行した。

| DB | 成功 | 失敗 | スキップ | カバレッジ |
| --- | ---: | ---: | ---: | ---: |
| SQLite | 1662 | 9 | 10 | 86.96% |
| PostgreSQL 16 | 1672 | 9 | 0 | 87.53% |

両方とも終了コード 1。元の全体実行を合格として扱わない。ローカル証跡は `tmp/full-c816f8e4-output/` の DB 別ログ、JUnit、カバレッジに保存した（Git 管理外）。

失敗は両 DB で一致した。

- CI の環境設定テスト 1 件：Playwright の `ENV_FILE` 未設定を要求していたが、独立した SQLite 検証のため明示的な空文字を設定済みだった。空文字と SQLite を検証するよう期待値を更新した。Compose 検証だけで `.env.example` を指定する検証は維持した。
- リポジトリ・文字品質検査 8 件（サブテストを含む）：Git archive の 847 ファイルは検証イメージと一致したが、`/candidate` に Git 管理情報がなく、Git コマンドが失敗した。アプリ機能の失敗とは区別する。

使い捨てコンテナ内で、生成物のないアーカイブの全ファイルを Git インデックスに登録し、修正したテストを読み取り専用で重ねて再検証した。対象は `test_docker_entrypoint.py`、`test_repository_hygiene.py`、`test_text_quality.py` の 3 ファイル。**32 件成功、4 サブテスト成功、警告 4 件、終了コード 0**。証跡は `tmp/full-fixture-regression.log`。ホストの Git 設定・インデックスには検証用の変更を加えていない。

この再検証は失敗箇所に限定しており、修正後の全体一括実行や GitHub CI の成功を示すものではない。実データ、DB スキーマ、アクセス権限、課金の変更はない。

## ブラウザとホームの HTML 挿入修正

同じ c816f8e4 の 111 ケースを Chromium・Firefox・WebKit、再試行なし、外部資産の取得可能な隔離環境で一括実行した。**106 成功・5 失敗、9.0 分、終了コード 1**。証跡は `tmp/browser-all-c816f8e4-final-output/results.json` と `tmp/browser-all-c816f8e4-final.log`。

2 件はホームで `/x` の画像取得が発生する失敗だった。グループ名・説明と履歴タイトルを未エスケープで HTML に挿入する箇所を確認し、DOM の `textContent` で文字をエスケープしてから表示するよう修正した。ほかの 3 件は WebKit のグループ一覧で対象カードを見つけられない失敗で、蓄積した公開グループによるページ分割にテストが対応していなかった。これらは別途修正・再検証する。

ホームの回帰テストは API 応答に画像タグとイベントハンドラーを含む文字列を注入し、修正前の Chromium で失敗を再現した。修正後は **3 ブラウザ 3 件成功、13.5 秒、再試行なし**。グループ名・説明・履歴タイトルの文字列一致、画像要素 0、イベントハンドラーの実行フラグなしを確認した。証跡は `tmp/home-security-red-verified.log`、`tmp/home-security-green.log`。API 応答を模擬した描画検証であり、全表示経路の安全性や修正後の全体一括合格を示すものではない。

## 失敗フローの再検証

招待承認後、所有者の権限操作、再読み込み後の参加者操作、非公開グループ作成後のテストを、画面のグループ名検索で対象カードを絞り込むよう修正した。作成後はモーダルが閉じることも確認する。カードや権限の検証は省略していない。

空の隔離 DB に開発用ユーザー 3 件と公開グループ 30 件を作成し、初めから一覧が複数ページになる条件で再実行した。対象は招待承認、管理者の付与・解除、非公開グループとセッション作成、主要画面の JS エラー検査、ホームの HTML 文字表示の 5 フロー。**3 ブラウザ計 15 件成功、2.1 分、再試行なし、終了コード 0**。証跡は `tmp/browser-regression.log` と `tmp/browser-regression-output/results.json`。

検証は c816f8e4 のブラウザイメージに、24233538 のホーム修正と更新したフローテストを読み取り専用で重ねて実施した。元の 111 ケース全体の失敗記録は保持し、15 件の再検証を全体一括成功に読み替えない。実環境への反映、実データ、課金、権限設定の変更は行っていない。自己レビューでは対象の検索・権限検証・日本語表示について追加の修正事項なし。

## ダッシュボード履歴の修正

6ee67201 後の監査で `templates/account/dashboard.html` に履歴タイトル・メモの未エスケープ挿入が残っていた。また、配列形式の履歴 API 応答を空の履歴として扱っていた。配列と results 形式の両方を受け取り、タイトル・メモを文字として表示するよう修正した。保存データや API の権限処理は変更していない。

模擬応答を使った Chromium の修正前テストは 2 件失敗。修正後は両形式の描画、実 API でのシナリオ・履歴作成から再読み込み後の表示、既存ホーム検査の **3 ブラウザ計 12 件が成功（23.4 秒、再試行なし）**。タイトル・メモの一致、画像要素 0、イベント実行フラグなし、保存済みメモの API 再取得を確認した。証跡は `tmp/dashboard-security-red.log`、`tmp/dashboard-security-green.log` と同名の `-output` ディレクトリ。

検証は c816f8e4 ブラウザイメージに対象テンプレートとテストを読み取り専用で重ね、空の隔離 SQLite を使った。実データ変更・本番反映なし。JavaScript 構文、差分、UTF-8、日本語表示を確認し、対象差分の自己レビューで追加指摘なし。全体 CI、外部連携、他の HTML 挿入経路の合格を意味しない。

## 共通の成功・エラーメッセージ

`static/js/arkham.js` の `showAlert` が渡されたメッセージを HTML として挿入していた。`textContent` を持つ span と閉じるボタンを DOM で作成する形へ変更した。閉じるボタンには日本語のアクセシブル名を付けた。既存の自動消去時間や表示位置は変更していない。

修正前の Chromium で失敗を再現し、修正後は **3 ブラウザ 3 件成功（19.6 秒、再試行なし）**。各ケース内で成功・エラー・API detail・API error の 4 経路を検証し、元の文字列の表示、画像要素 0、イベント実行フラグなし、閉じるボタンで消えることを確認した。証跡は `tmp/alert-security-red.log`、`tmp/alert-security-green.log` と対応する出力ディレクトリ。テストは共通 UI 関数に入力を渡すもので、実外部 API のエラー配送や全呼出元の検証を意味しない。

空の隔離 DB と c816f8e4 ブラウザイメージに対象 JS・テストを重ねて実行した。構文・文字品質・差分・日本語表示を確認し、自己レビューで対象差分の追加指摘なし。DB・秘密情報・費用への変更なし、本番未反映。GitHub CLI 認証はこの時点でも未設定であり、リモート CI の成功は未確認。

## セッション招待の文字表示

6899f690 後の監査で、セッション一覧の招待タイトル・招待者名に未エスケープ挿入を確認した。招待メッセージと同じ既存の `escapeHtml` に通して文字として表示するよう修正した。

修正前 Chromium の 1 件失敗を再現し、修正後は **3 ブラウザ 3 件成功（16.9 秒、再試行なし）**。模擬の招待 API 応答を使い、タイトル・招待者名・メッセージの一致、画像要素 0、イベント実行フラグなし、「承認して参加」「辞退」ボタンの表示を確認した。承認・辞退の API 操作や実配送はこのテストの対象外。証跡は `tmp/session-invite-red.log`、`tmp/session-invite-green.log` と対応する出力ディレクトリ。

6899f690 のブラウザイメージに対象テンプレートとテストを重ね、空の隔離 DB で実行した。JavaScript 構文確認は Django の `user.id` を検証用の数値に置き換えた抽出スクリプトで成功し、実レンダリングはブラウザで確認した。差分・UTF-8・日本語表示・自己レビューで追加指摘なし。実データ、DB、秘密情報、費用の変更なし、本番未反映。

## 固定候補の再検証環境と PR の制約

6899f690 の Git archive 全 852 ファイルとブラウザイメージのファイル内容を SHA-256 で全件照合し、一致した。イメージは `tableno-browser:6899f690`、ID は `sha256:46fb49b6efd89eaadb4fafc107640ea4a40382c92f24d3d462e38b9845cd433e`。ソースの上書きなしでブラウザ全体を実行する。

この全体実行は **125 件成功・1 件失敗、10.1 分、再試行なし、終了コード 1**。Firefox の秘匿ハンドアウトフローが 90 秒で停止した。画面は最初の参加者の公開グループ参加ボタンを表示した段階で、サーバーログにもグループ参加 POST やハンドアウト作成は記録されていない。後処理の context.close エラーが表示されており、元の停止原因を特定できていない。秘匿情報保護の成功にも漏えいの証拠にも読み替えない。証跡は `tmp/browser-all-6899f690.log` と `tmp/browser-all-6899f690-output/results.json`、失敗画面・動画。

同じ固定イメージで公開グループ 30 件を準備し、フローを 3 ブラウザ各 2 回、操作トレース付き・再試行なしで実行する診断を開始した。`tmp/handout-diagnostic.log` と `tmp/handout-diagnostic-output/` に保存する。この追記時点では診断結果待ち。

診断は **6 件すべて成功（1.8 分）**。各実行のトレースを保存したが、元の停止は再現せず、原因解消・全体合格とは扱わない。

元の全体実行では再試行を無効にしていた一方、Playwright の設定が `on-first-retry` だったため、初回失敗のトレースがなかった。`retain-on-failure` に変更し、成功時は破棄、初回を含む失敗時は保持する。[公式のトレース設定](https://playwright.dev/docs/test-use-options#trace-modes)を確認し、固定イメージの Playwright 1.53.1 でも動作検証した。

アプリや DB を使わない使い捨ての成功・意図的失敗各 1 件を再試行なしで実行した。設定変更前は trace.zip 0 件、変更後は失敗ケースだけに 1 件保存され、アーカイブ内に該当の失敗情報があることを確認した。両実行の終了コード 1 はこの検証入力による想定結果であり、アプリテスト失敗の無視ではない。入力テストはリポジトリに追加していない。証跡は `tmp/trace-retention-before.log`、`tmp/trace-retention-after.log` と各出力ディレクトリ。記録処理の負荷は増えるが、再試行回数・テストの合否条件は変えない。

バックエンドは a95e1a20 の全 853 ファイルが一致する `tableno-full:a95e1a20`（ID `sha256:0e90d02f6f7ec4e5bf51a2cbd1054a718d6f22ca071ee9a1e89156f3907f6d51`）を作成した。コンテナ内の非 root ユーザーがアーカイブから検証専用 Git インデックスを作成することで、前回の Git 管理情報不足を解消した。事前のリポジトリ・文字品質・CI 設定検査は 32 件・4 サブテスト成功。SQLite と PostgreSQL 16 の全体検証を開始したが、この追記時点では結果待ちであり合格扱いにしない。出力先は `tmp/full-a95e1a20-output/`。PG はポート公開なし・内部ネットワーク・tmpfs の使い捨て DB である。

GitHub 接続で対象ブランチの PR を検索し、該当なしを確認後、Draft PR 作成を再試行した。`403 Resource not accessible by integration` で拒否された。GitHub CLI は未認証、利用可能なアプリ内ブラウザも GitHub 未ログインだった。PR は作成できておらず、リモート CI の成功も未確認。通常の SSH push は成功している。権限・認証設定を変更せず、確認用ブラウザタブは閉じた。

## カレンダーのタイトル表示

カレンダーの次回セッションと既存セッション選択肢がタイトルを HTML に挿入していた。既存の `escapeHtml` を適用し、タグを含むタイトルも文字列のまま表示するよう修正した。

修正前 Chromium で 1 件失敗を再現し、修正後は **3 ブラウザ 3 件成功（21.2 秒、再試行なし）**。模擬 API 応答を使い、次回セッションのタイトル一致、画像要素 0、イベント実行なし、新規セッションモーダルから「既存セッションに日程を追加」への切替、選択肢のタイトル一致と選択を確認した。日程追加の保存はこの検査に含めない。証跡は `tmp/calendar-title-red.log`、`tmp/calendar-title-green.log` と対応する出力ディレクトリ。

6899f690 の固定イメージに対象テンプレートと検査を読み取り専用で重ね、隔離 DB で実行した。抽出 JavaScript の構文、差分、UTF-8、日本語表示を確認し、対象差分の自己レビューで追加指摘なし。保存データ・DB・秘密情報・費用変更なし、本番未反映。実行中の a95e1a20 バックエンド全体検証には、この後続のテンプレート修正は含まない。

## 参加者管理の名前・ゲスト・リンク表示

セッション一覧から開く参加者管理に、参加者名・キャラクター名の未エスケープ挿入と、ゲストの `user_detail=null` による表示停止を確認した。表示名、ニックネーム、ユーザー名、ゲストの順で安全な文字表示に統一した。キャラクターシートリンクは URL として解析できる HTTP/HTTPS のみ表示し、同じアプリ内の相対 URL も解決する。属性をエスケープし、別タブには `noopener noreferrer` を指定する。

最初のテストはアイコン付きボタンの名前完全一致で停止したため、その実行を中止して画面内の対象ボタンにセレクターを修正した。アプリ変更前の再検証では登録ユーザー名・ゲスト表示の 2 件が失敗した。証跡は `tmp/participant-display-red.log`（テスト側の停止）と `tmp/participant-display-red-verified.log`（アプリ側の再現）。

修正後は **3 ブラウザ 9 件成功（30.6 秒、再試行なし）**。模擬 API 応答から参加者ボタンをクリックし、名前・キャラクター名の一致、画像要素 0、イベント実行なし、HTTPS リンクの維持、javascript/data URL の非表示、壊れた URL・空 URL の非表示、名前未設定ゲスト・ユーザー名への代替表示、相対リンクの解決を確認した。初期の 6 件成功は `tmp/participant-display-green.log`、追加ケースを含む最終結果は `tmp/participant-display-final.log` と対応する出力ディレクトリ。

6899f690 イメージに対象テンプレート・テストを重ねた隔離検証であり、実データ保存や外部リンク先へのアクセスは行っていない。削除・参加者権限の API は変更していない。JavaScript 構文、差分、文字品質、日本語表示と自己レビューで対象差分の追加指摘なし。DB・秘密情報・費用変更なし、本番未反映。

## a95e1a20 バックエンド全体の完了結果

前述の結果待ちだった実行は、両 DB とも終了コード 0 で完了した。

| DB | 成功 | スキップ | 成功サブテスト | 警告 | カバレッジ | 時間 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| SQLite | 1669 | 10 | 480 | 159 | 86.96% | 1378.12 秒 |
| PostgreSQL 16 | 1679 | 0 | 480 | 159 | 87.54% | 1409.78 秒 |

JUnit は両方とも testcase 1679 件、failure/error 0。SQLite でスキップされた 10 件の classname/name を PostgreSQL の結果と照合し、すべて成功していることを確認した。アプリ 5 領域の集計カバレッジは閾値 70% を超えたが、全行・全分岐の検証や本番設定の合格を意味しない。実行範囲は accounts、api、scenarios、schedules、support、tableno、tests/unit、tests/integration。`APP_ENV=local` と選択した DB を使う隔離検証であり、GitHub CI、実 AWS、本番反映の証明ではない。

証跡は `tmp/full-a95e1a20-output/` の DB 別ログ、JUnit、XML/JSON カバレッジ。対象は a95e1a20 に固定され、その後のトレース設定・カレンダー・参加者管理の変更を含まない。c816f8e4 の旧全体失敗と 6899f690 のブラウザ全体失敗の記録は維持する。

2ec4ccc5 の全 855 ファイルが一致するブラウザイメージ `tableno-browser:2ec4ccc5`（ID `sha256:f4d70242537849658ae4dad411b2e9176b2332acb2600fd6ae224de4b784850d`）で、後続修正を含む 141 ケースの一括検証を開始した。再試行なし、初回失敗のトレース保持あり。この追記時点では実行中。証跡先は `tmp/browser-all-2ec4ccc5.log`、`tmp/browser-all-2ec4ccc5-output/`。

## セッション詳細の選択欄

日程参加者のチェック欄とキャラクター選択欄に、名前の未エスケープ挿入が残っていた。既存の `escapeHtml` を適用し、チェックボックスの値やキャラクター ID の扱いは維持した。

修正前 Chromium の 2 件失敗を確認し、修正後は **3 ブラウザ 6 件成功（33.2 秒、再試行なし）**。隔離 API でグループ・セッションを作成し、メンバー名・キャラクター名の取得応答を模擬した。日程追加ボタンからモーダルを開き、参加者名の一致、画像要素 0、イベント実行なし、チェックボックスの選択、キャラクター選択肢の名前一致を確認した。日程・キャラクター選択の保存はこの検査の対象外。

証跡は `tmp/session-choice-red.log`、`tmp/session-choice-green.log` と対応する出力ディレクトリ。2ec4ccc5 のイメージに対象テンプレートと検査を読み取り専用で重ねて実行した。実レンダリング、差分、UTF-8、日本語表示と対象差分の自己レビューで追加指摘なし。DB・秘密情報・費用変更なし、本番未反映。並行する 2ec4ccc5 の全体検証には、この後続修正を含めない。

## ハンドアウトテンプレート本文の一致

ハンドアウトテンプレート一覧 GET は本文全体を返していたが、テンプレートから作成する POST は別定義の仮本文（省略記号付き）を保存していた。一覧と作成で同じ定義を使うよう修正した。不正な customizations（配列・null）はサーバー例外ではなく400応答で拒否し、未知のテンプレートも日本語で拒否する。GM権限・参加者とセッションの整合検証は維持した。

修正前は本文一致3種類と不正入力2種類の計5サブテストが失敗。修正後は `schedules/test_handout_management.py` と `schedules/test_handouts.py` の **30件・6サブテストが成功（100.32秒、警告9件）**。本文の保存結果と一覧の完全一致、既定の秘匿設定、未知テンプレート・不正入力時の保存なしを確認した。証跡は `tmp/handout-template-red.log`、`tmp/handout-template-green.log`。

a95e1a20の検証イメージに対象ソース・テストを重ね、ネットワークなしの隔離SQLiteで実行した。Black・isort・flake8、差分、文字品質、日本語文言、自己レビューを確認した。既存ハンドアウトの書換えやDB移行は行わない。秘密情報・費用変更なし、本番未反映。この後続修正はa95e1a20の全体合格と2ec4ccc5のブラウザ全体実行には含まない。

## 2ec4ccc5 ブラウザ全体の完了結果と操作診断

前述の実行中だった141件は、**139件成功・2件失敗（12.1分、再試行なし、終了コード1）**で終了した。失敗はいずれもWebKitで、無料ユーザーのCCFOLIAインポートと、ホームの利用規約リンクから法務ページへ移動するフローだった。ソースは2ec4ccc5に固定され、後続の1295ac07・ce50702eの変更は含まない。

- インポート: 送信ボタンのclick処理は完了したが、対応するPOST応答待ちがタイムアウトした。失敗時も入力モーダルが表示されていた。クリック完了だけを実際のフォーム送信の証拠として扱わない。
- 利用規約: リンクのclick処理は完了したが、URLがホームのまま変わらなかった。入力時と操作後のトレースでHTMLのスクロール位置が1654から1671へ変化していた。スクロール継続は確認できるが、クリック失敗の原因と断定する材料は不足している。

初回失敗のtrace.zip・画面・動画を `tmp/browser-all-2ec4ccc5-output/results/` に保持している。集計は同ディレクトリの親にあるresults.json、サーバーログは `tmp/browser-all-2ec4ccc5.log`。

同じ固定イメージと空の隔離SQLite、開発用ユーザー3件で、2つのフローをWebKit各3回、再試行なしで診断した。テストにpointerdown/pointerup/click/invalid/submitの対象要素・座標・スクロール位置だけを記録する処理を加え、アプリは変更していない。**6件成功（43.5秒、終了コード0）**で、元の失敗は再現しなかった。成功時には送信ボタンのclickとformのsubmit、および法務リンクのclickを確認した。ページの算出済みscroll-behaviorはsmoothだったが、これだけで失敗原因とは判断しない。

診断の証跡は `tmp/click-diagnostic.log` と `tmp/click-diagnostic-output/results.json`。診断は全体実行で蓄積したデータを再現しておらず、操作記録の追加によるタイミングへの影響も否定できない。次に全体実行のデータ量・描画とスクロールのタイミングを再現し、実イベントの対象と送信・遷移の有無を確認する。6件成功を全体合格や原因解消に読み替えず、公開判定はNo-Goを維持する。

この追記ではアプリ修正・実データ変更・DB移行・秘密情報・費用変更・本番反映を行っていない。診断用コンテナは終了時に破棄した。文書の集計・証跡パス・記述・差分を自己レビューした。

## データ量を増やした操作診断と現候補の再検証

2ec4ccc5の同じブラウザイメージに、空の隔離SQLite・開発用ユーザー3件・公開グループ30件・adminの所属6件・将来セッション10件を用意し、操作記録付きの2フローをWebKitで各5回実行した。**10件成功（1.2分、再試行なし、終了コード0）**。adminのホームではスクロール位置1818で法務リンクのpointerdown/pointerup/clickを確認した。前回より長い画面でも失敗は再現せず、データ量やスクロールだけを原因とは判断できない。無料ユーザーは各回新規登録し、実APIでCCFOLIAインポートする既存の検査を維持した。

証跡は `tmp/click-volume.log` と `tmp/click-volume-output/results.json`。元の全体実行と同じデータ内容・実行順序ではないため、この10件成功を全体合格に読み替えない。診断用コンテナとDBは終了時に破棄した。

現在の8f4bbc5dから作った `tableno-browser:8f4bbc5d`（ID `sha256:e2de82482d82fe13c3591916ffe0aa8c4e35c170ed6c0cbc858100f7d6c47b20`）は、Git archiveの856ファイルと内容が全件一致した。アプリソースを上書きせず、free-kp-featuresとsmokeのテストにのみ前述の操作記録を重ね、再試行なしでブラウザ全体の検証を開始した。元のテスト本文は2ec4ccc5から変更されていないことを確認した。後続のセッション選択欄・ハンドアウトテンプレート修正を含むが、この追記時点では実行中であり合格扱いにしない。証跡先は `tmp/browser-all-8f4bbc5d.log` と `tmp/browser-all-8f4bbc5d-output/`。

初回のアーカイブ展開はホストPythonのextractall引数非対応で停止した。展開先配下の通常ファイル・ディレクトリだけであることを事前検査する形に改めて展開・ビルド・内容照合を完了した。これは検証準備の失敗であり、アプリテストの合否には数えない。

## 8f4bbc5d 全体検証の完了と履歴APIのキャッシュ修正

前述の147件は **146件成功・1件失敗（10.3分、再試行なし、終了コード1）** で終了した。Firefoxの「保存した履歴をダッシュボードに表示する」検査が失敗した。前回の無料インポート・法務リンクは成功したが、過去の停止原因を解消した証拠とはしない。

失敗トレースでは履歴POSTが201で保存された後、再読み込みによる一覧GETが以前と同じ本文を返した。後者は_transferSize=0、time=0で、API応答にはCache-Controlがなく、画面にも以前の履歴だけが残った。個人の履歴APIを `never_cache` で保護し、一覧・詳細・作成・更新・削除と拒否応答をprivate/no-store/no-cache/max-age=0にする。所有者の絞り込み、認証、保存内容は変更しない。

新規のキャッシュ・権限検査は修正前に2件失敗した。初回の修正後検証は37成功・1失敗で、追加したテストの未認証応答の期待値が403だった。既存設定はTokenAuthenticationを先頭に持つため401が正しく、アプリ側は変更せず期待値を訂正した。最終的に `scenarios/test_history_cache.py`、`scenarios/test_scenarios.py`、`scenarios/tests.py` の **38件・3サブテスト成功（56.50秒、警告9件）**。一覧/詳細、更新内容、作成、削除後404、他ユーザー404、未認証401とキャッシュ禁止を確認した。

修正後の `home-content-security.spec.ts` は **3ブラウザ各2回、計24件成功（42.1秒、再試行なし）**。模擬APIの配列/ページ形式とホームの文字表示、実APIで保存後の履歴・メモ表示と再取得を確認した。ブラウザは8f4bbc5d、バックエンドはa95e1a20の固定イメージに対象ソース・新規テストを読み取り専用で重ね、隔離SQLiteで実行した。24件成功を修正後の147件全体成功には読み替えない。

証跡は `tmp/history-cache-red.log`、`tmp/history-cache-green.log`（期待値の誤り）、`tmp/history-cache-final.log`、`tmp/history-cache-browser.log` と `tmp/history-cache-browser-output/results.json`。元の全体失敗のtrace.zipは `tmp/browser-all-8f4bbc5d-output/results/home-content-security-dash-3a80b-ys-persisted-history-safely-firefox/` に保持した。

Black・isort・flake8、差分・UTF-8・日本語文言と自己レビューを確認した。履歴APIのキャッシュ再利用を止めるため毎回取得が必要になるが、DB移行・既存データ変更・権限変更・秘密情報・費用変更はない。実環境未反映で、既に別クライアントに保存されたコピーの消去を保証しない。

## f016a66dの本番用イメージとブラウザ全体

f016a66dのGit archiveから通常Dockerfileで本番用イメージを作成し、隔離PG16/Redis7・本番設定で通常起動、移行、静的配信、check --deploy等を確認した。初回の仮Stripe設定の形式エラーと訂正、ソース一致496ファイル、検証範囲と限界は [配備準備資料の現候補](AWS_PRE_FORMAL_RELEASE_VALIDATION_PLAN.md) に記録した。実環境への配備・実決済・実OAuth/S3配信・実データ復旧はこの検証に含まない。

ブラウザ用の `tableno-browser:f016a66d`（ID `sha256:0b2223cf8edcdb87edc32b7d0895d53a39add2b98d52b21c1b0523e66bec818e`）はarchiveの857ファイルと全件一致した。今回はアプリ・テストの上書きなしで147件を開始し、再試行なし・失敗時trace保持を維持した。空の隔離SQLiteと開発用ユーザー3件を使用し、外部資産取得可能なネットワーク条件。この追記時点では実行中で、合格扱いにしない。証跡先は `tmp/browser-all-f016a66d.log` と `tmp/browser-all-f016a66d-output/`。

## f016a66d ブラウザ全体の完了結果

前述の実行は **147件すべて成功（602.80秒、約10.0分、終了コード0）** で完了した。results.jsonを集計し、23フローファイル、Chromium/Firefox/WebKit各49件、skip=0、unexpected=0、flaky=0、全147結果のretry=0を確認した。ソース・テストの上書きなしで、履歴修正を含むf016a66dの結果である。その後の1c8814e3は文書のみの変更。

以前失敗した履歴表示・無料CCFOLIAインポート・法務リンクと秘匿ハンドアウトのフローも含め、今回は成功した。キャッシュ修正の結果と、原因未確定の過去のクリック停止を混同しない。過去の失敗ログと診断記録は削除せず、再発時の比較材料として維持する。この成功は隔離ブラウザフローの一括合格であり、実サービス・実機・全状態の網羅や正式公開条件全体の達成ではない。

2026-09-06、現在の文書コミット1c8814e3に対するGitHubのPR起動ワークフロー一覧とcombined commit statusを読み取り、両方とも空を確認した。API応答の成功はCI実行成功を意味せず、ワークフロー取得ツールはPR起動分の最初のページに限定される。CLI/連携の権限変更やPR作成再試行は行っていない。リモートCIの成功は未確認のまま。

全体検証コンテナと隔離DBは終了時に破棄された。今回の更新は集計・対象SHA・証跡と残条件の文書反映であり、DB・秘密情報・権限・費用・本番への変更はない。

同じf016a66dの857ファイルが一致するバックエンド用イメージ `tableno-full:f016a66d`（ID `sha256:beaf6f4b3329b01cd83b095c8fc3b7ae7552974d2d3cbc99c18c42af55f8fb17`）を作成した。検証用Gitインデックスは非rootユーザーがarchiveから作成する。APP_ENV=local、ENV_FILE空で、accounts/api/scenarios/schedules/support/tableno/tests/unit/tests/integrationをSQLiteとPostgreSQL16で実行開始した。PGは公開ポートなし・内部ネットワーク・tmpfsの専用DBで、実データを使用しない。出力先は `tmp/full-f016a66d-output/`。この追記時点では両方実行中であり、a95e1a20の全体結果を置き換える成功証拠はまだない。

## f016a66dのCI静的チェック

同じ固定バックエンドイメージの/candidateで、CIと同じ `flake8 .`、`black --check .`、`isort --check-only .` を順に実行し、すべて終了コード0。Blackは526ファイルを変更不要と判定した。ソースや設定の上書きなし、外部通信なしで、証跡は `tmp/static-ci-f016a66d.log`。これは静的3チェックの成功であり、GitHub Actionsの実行成功ではない。

CIと同じ `bandit -r accounts schedules scenarios tableno` は解析エラー0、HIGH/MEDIUM 0、LOW555、終了コード1。内訳はB311=66、B106=396、B110=2、B105=82、B404=1、B607=4、B603=4。証跡は `tmp/bandit-f016a66d.log` と `tmp/full-f016a66d-output/bandit.json`。

過去c3996913のLOW556はsupport/tests.pyのB106を1件含む範囲だった。今回のCIコマンドはsupportを走査しない。同じ範囲に絞り、ファイル名の区切り・行番号変動を正規化して、ファイル/検査種別/指摘されたソース行の多重集合を照合すると555件は完全一致した。周辺のテスト名や画像上限変更による差分はあるが、指摘自体の追加・削除はない。1件減を不具合解消とは数えず、警告抑制やCIの合否条件変更も行っていない。既存の用途判定と実環境の未完了事項は [静的指摘の分類記録](SECURITY_STATIC_TRIAGE_494ABD0D.md) を参照する。

## 指定Googleアカウントでの再ログインと追加権限の警告

ユーザーがChromeで開いたGoogleカレンダーのアカウントを検証に使用することを許可した。Chromeの実画面で対象アカウントを確認し、stg.tableno.jp/integrations/からGoogleログインへ進んだ。Googleは既存のtableno.jpへの再ログインと表示し、同じアカウントを選択した結果、開発環境の既存ユーザー「しぇぱ」の連携設定へ戻ることを確認した。これは実Googleの既存アカウント再ログインの証拠であり、新規登録・アカウント競合・失効・現在のローカル候補SHAの配備確認ではない。

「Googleを再連携」の要求scopeはcalendar.eventsとspreadsheetsの両方を含む。同じアカウントを選択すると、Googleが「このアプリは Google で確認されていません」と表示し、開発者による審査リクエストへの案内が出た。警告を越える操作や新しい権限の許可は行わず、Chromeのタブをユーザー確認用に残した。予定の同期、スプレッドシート出力、既存予定の変更、招待送信は行っていない。アカウント識別子・OAuth状態・認証URLのクエリ・トークンは文書へ保存しない。

追加権限の実検証はユーザーの画面確認待ちであり、正式公開前にはこの警告の原因となるGoogle側の公開・審査状態も確認が必要。ブラウザでのGoogle再ログイン成功を、Driveコネクターの認証回復やGoogle Calendar/Sheets同期成功には読み替えない。

## ユーザーのGoogle連携操作後の再確認

ユーザーから「連携しました。確認してください」と連絡を受け、同じChromeの開発環境を確認した。ログインは維持されていたが、連携設定は「未連携」、Calendar/Sheetsとも無効だった。承認済みのCalendar同期をチェックして「Google設定を保存」を押しても、保存完了の表示は出なかった。「再読み込み」後にはCalendarのチェックが外れ、「未連携」のままだった。連携ジョブは0件で、予定同期・シート出力・招待送信は実施していない。

これにより、追加認可についてのユーザー操作待ちは解消したが、タブレノ側の設定保存が成立していないことを確認した。HTTP応答・サーバーログ・保存済みトークンはこの確認では取得しておらず、原因やGoogleが実際に付与したscopeは未確定。現行ソースの設定APIはSocialTokenの存在とSocialAccount.extra_dataのscopeを要求しており、認可結果の保存・取得経路と保存エラーの画面表示を追跡する必要がある。ローカル候補と稼働版の一致も未確認のため、ローカル実装の読み取り結果だけで実環境の原因を断定しない。

Chromeには連携設定画面を残した。認証情報・個人の予定・OAuthクエリは記録せず、アプリ配備・DBの直接変更・権限設定変更は行っていない。I04/I05の実同期・出力の受け入れ条件は未達のまま。

## f016a66d バックエンド全体の完了確認

先に開始した両DBの実行について、セッションハンドルと実行コンテナは終了済みだった。Dockerのdieイベントを元コンテナIDで照合し、SQLiteの13c8d57f4ab9、PostgreSQLの3e06ad15ccccがともに終了コード0であることを確認した。ログの末尾には全体集計が残っていなかったため、保存済みJUnitとcoverage JSONを直接集計した。

- SQLite: testcase要素1683件、うちスキップ10件、失敗・エラー0件。実行された1673件が成功。JUnit所要時間1260.431秒、カバレッジ86.98%。
- PostgreSQL16: testcase要素1683件、スキップ・失敗・エラー0件。1683件成功。JUnit所要時間1287.482秒、カバレッジ87.56%。

同じclassname/nameの重複はなく、SQLiteでスキップした10件はPostgreSQL側に存在して成功している。testsuiteのtests属性2169はtestcase要素数と異なるため、受け入れ資料の件数にはtestcase要素の集計を使用した。過去a95e1a20の警告数やサブテスト数を今回の結果へ流用しない。証跡は `tmp/full-f016a66d-output/{sqlite,postgres}-junit.xml` と各coverage.json/xml。

対象は前述の857ファイル一致を確認したf016a66dで、ハンドアウトと履歴キャッシュ修正を含む。以後のGoogle設定保存UI変更は含まない。専用PGはtmpfs・内部ネットワークを再確認して停止し、専用ネットワークも削除した。リモートCI、実OAuth/決済/配備の成功証拠ではない。

## Google設定保存の失敗表示と再試行

Google設定の保存処理に例外処理を追加した。400では追加権限の確認、401/403では再ログイン、それ以外のHTTPエラー・通信断では保存状態の再確認を日本語で案内する。応答本文や認証情報は表示しない。保存中はボタンと対象チェックボックスを無効化し、15秒の通信タイムアウト後も操作できるようfinallyで復帰する。成功時はPUT応答で表示を更新し、保存後のGET失敗を保存失敗と混同しない。

初回の隔離テスト起動では開発ユーザーの表示条件を満たさず、ログイン画面で停止した。使い捨てfixtureを管理者に訂正後、修正前のChromium検証は保存失敗の案内が空のため1件失敗した。修正後はまず15件成功し、403と保存中の操作制限・追加GET不要の検査を加えた最終版は **3ブラウザ各7件、計21件成功（59.7秒、再試行なし、終了コード0）**。認可不足、401、403、500、通信断からの再試行、成功表示、応答本文の非表示、未処理のpageerrorがないことを確認した。実時間15秒のタイムアウト発火はこのテストでは未検証。

固定 `tableno-browser:f016a66d` に対象テンプレートと新規テストを読み取り専用で重ね、空の隔離SQLiteと開発fixtureを使用した。外部連携API応答はブラウザで模擬しており、実Googleへの送信や保存成功の証拠ではない。証跡は `tmp/google-save-red.log`、`tmp/google-save-green.log`、各 `*-output/results.json` と修正前のtrace。テンプレートのJavaScript構文、差分、UTF-8/LF、日本語文言と自己レビューを確認した。DB・権限・Secrets・費用の変更はなく、共有環境には未反映。復旧はこのUI修正のrevertで可能。

認可経路の調査では、固定イメージ内のallauth標準GoogleOAuth2Adapter.complete_loginがIDトークンまたはuserinfoだけからextra_dataを作り、トークン応答のscopeを引き継がないことを確認した。STORE_TOKENSの既定値はFalseで、リポジトリには有効化設定がない。一方、Google設定APIはSocialTokenとextra_data.scopeを要求し、既存テストはそれらを直接DBへ作っている。実コールバックからの保存を再現するテストと修正が必要であり、今回のUI修正でその不足を解消したとはしない。稼働環境の設定・応答との照合も残る。
