# 通常配布物による一覧APIの性能再測定

## 結果

セッション変換処理を改善した7b196988を通常Dockerfileでビルドし、前回と同じ登録100人・同時10件の混在GETを3回測定した。各API各回100件、計900件がHTTP200で、全9組のp95が3秒以内だった。今回の代表データと隔離ローカル構成での一覧APIは基準内。保存・編集・実AWSを含む性能ゲート全体は未完了。

| API | 1回目p95秒 | 2回目p95秒 | 3回目p95秒 |
| --- | ---: | ---: | ---: |
| キャラクター一覧 | 2.116 | 2.100 | 2.219 |
| シナリオ一覧 | 1.900 | 1.906 | 1.907 |
| セッション一覧 | 2.092 | 2.011 | 2.110 |

事前の単発ウォームアップ300件もHTTP200。前回のセッション一覧p95（6.691/6.209/3.706秒）に対して改善を確認できた。シナリオ一覧も今回は3回とも基準内だが、シナリオ処理自体を変更したわけではなく、混在負荷の競合も影響し得るため独立した高速化とは主張しない。

## 同条件の確認

- Gitのクリーンなアーカイブ7b1969884ad6f89b59417a2ff6c3829c1ed3f6ceからビルド。イメージ `aws-pre-7b196988`、ID `sha256:e206c02ffe3b2422236e30a9f74dfee938297903bfb32a54ae2bd9e278e1a9ac`。修正ファイルの後付けマウントは使用していない。
- 前回のfa09731cとベース・依存部分の10レイヤーが一致。アプリの実装差分はschedules/serializers.pyで、その他はテスト・文書。
- アプリ0.25 CPU/512 MiB、PG18.3 2 CPU/1 GiB、Redis7 128 MiB。空の一時DBへ登録100人・キャラクター1,000件・セッション1,000件・シナリオ1,000件・グループ100件を作成。前回と同じseedと設定を使用し、識別用のリソース名だけを変更した。
- 先頭ユーザーの認証付きGETでキャラクター10件、セッション20件、シナリオ30件を返すことを確認。
- 同じread_load.pyで100人のToken認証情報を順次利用し、3種類のGET全体の同時上限を10にした。HTTP完了までをnearest-rankで集計。クライアントの待ち時間を挟まない負荷であり、人間10人の画面操作モデルと同義ではない。
- 全試験用コンテナ・内部ネットワークは所有ラベルを照合して削除し、残存なしを確認。実ユーザーデータ・AWSリソース・共有DB・権限・費用は変更していない。

証跡はGit管理外の `tmp/performance-100-7b196988/`（run.py、seed.py、check.py、seed.log、fixture-requests.log、warmup.log、concurrency-10-a/b/c.log、server.log）、ビルドログは `tmp/aws-pre-7b196988-build.log`。使い捨てenvとトークンはコミットしない。

## 残条件

通常配布物での一覧API改善は確認できたが、この試験には保存・編集・画像アップロード・長時間負荷・TLS・実RDS/S3・ブラウザ描画は含まない。正式公開No-Goを維持する。AWSへのこの修正の反映は未実施。

7b196988の[CI run34431934642](https://github.com/sheepdog0820/iaia/actions/runs/34431934642)は2026-09-10にGitHubのjobs APIで全5ジョブ（system、production-database、lint-security、Unit / Integration、playwright）のcompleted/successを確認した。CI待ちは解消したが、上記性能試験の対象範囲が広がるものではない。
