# 退会後の実署名付き購読通知

2026-09-19、実装・ブラウザーテスト候補4f068ca6を隔離DBと承認済みStripeサンドボックスで検証した。[結果JSON](STRIPE_DELETION_SIGNED_2026-09-19.json)の13項目すべて成功。

## 手順と結果

1. 新しいテスト顧客と無課金の試用契約を作成し、両方のlivemode=falseを確認した。
2. 専用Stripe CLI listenerが転送した実際の契約作成通知を、本文・署名を改変せずメモリー内に保留した。ローカルDBにはまだ契約状態を反映していない。
3. 退会サービスを呼び出すと、Stripeに残る契約を検知して拒否し、利用者を保持した。
4. この試験の契約をサンドボックスで解約し、実際の契約削除通知も保留した。アプリの退会サービスから利用者を削除し、通知処理記録がまだ0件であることを確認した。
5. 削除通知、古い作成通知の順に元の署名付き本文をWebhookへ渡した。いずれもHTTP 200・処理成功になった。
6. 同じ通知を作成・削除の順に再送してもHTTP 200だった。利用者・購読レコードは再作成されず、成功したイベント記録は2件のままだった。

試験後にStripeの契約がcanceledであることを再取得した。CLI listenerと一時HTTPサーバーを終了し、隔離DBを破棄した。専用ポート8916の待受も残っていない。実課金、実利用者へのメール、共有DB、main/AWS、Secrets、継続費用の変更はない。生の通知本文・署名・APIキーを証跡へ保存していない。

## 限界と統合

これは実署名付き通知を退会完了後に遅延・逆順・重複処理する試験であり、退会トランザクションとWebhookの同時処理や、実Stripe契約を伴うブラウザー退会を検証したものではない。期間末までの時間経過も再現していない。これらとAWSでの確認は残る。

今回、作業ブランチ `codex/stripe-evidence-20260919` へ4f068ca6を統合し、先行するコンテナ・CI・Checkout通知の証跡と退会実装をまとめた。受け入れ一覧のB03・B04だけが競合し、退会の最新証拠と先行Webhook証拠の双方を残して解消した。docs以外のファイルは4f068ca6と同一であることをgit差分で確認した。mainへのマージではない。

旧候補99acd8caのCI成功を新候補へ流用しない。反映承認の対象も無断で変更しない。正式公開判定は引き続きNo-Go。
