# mainマージ前の追加検証

## 例外の黙殺の是正

旧マイグレーション0041のストレージ削除失敗を、秘匿情報を含めない警告として記録する。過去の移行順序・スキーマ・best-effortの継続動作は維持する。適用済み環境で再実行される変更ではなく、過去に残ったファイルの削除も行わない。既存ストレージの照合・整理は正式公開前の残作業である。

開発用画像生成では、フォントが扱えない文字の失敗だけを警告付きで許容し、予期しない例外は伝播させる。文字列・例外本文はログへ出さない。

回帰テスト3件を修正前に失敗確認、修正後に成功確認した。DBへのアクセスを禁止するSimpleTestCaseとmockストレージを使用し、共有DB・実ファイルは変更していない。

## Banditの個別判定

accounts/api/schedules/scenarios/support/tablenoを検査し、例外処理修正後のLOW559件を確認した。既存の[分類記録](SECURITY_STATIC_TRIAGE_494ABD0D.md)をもとに、隔離テストの固定値・模擬OAuth応答、ローカル限定コマンドのサンプル値、ゲーム用乱数、公開URL、真偽値、フォームクラス名を対象行のルールIDに限ってnosecで注記した。追加のGoogle callback試験も実トークン交換をpatchしていることを確認した。テスト用Node起動は固定スクリプトと引数配列で、信頼されたテスト環境のPATHを前提とする。

ディレクトリ除外・LOW全体の除外・終了コードの成功化は行わない。100ファイルのASTを66f016f9と比較し、実行処理の差分0を確認した。注記に伴うBlack整形と既存BOMの除去を含む。2026-09-06の再検査はBandit指摘0・解析エラー0・終了0。依存監査は検証用イメージの207パッケージを対象に脆弱性0・終了0。証跡はtmp/bandit-merge-final.jsonとtmp/pip-audit-merge.json。

この判定は過去のストレージ残存ファイルや実サービスの検証完了を意味しない。新しい指摘や注記対象の処理変更は改めてレビューする。

## リモートCI

PR作成前にも同じ必須ジョブを確認できるよう、Django CIのpush対象にcodex/**を追加する。既存のmain・PRトリガーは維持し、検証ジョブの省略は行わない。Bandit対象をAPIとsupportを含む6アプリへ拡大する。このworkflowにデプロイ・共有DB接続・外部通知のジョブはない。

初回リモートCIでは静的解析ジョブが成功したが、Django初期化のジョブが失敗した。Git管理ファイルだけを展開し、CIと同じ環境変数で実行すると、manage.pyが存在しない.env.developmentを要求する失敗を再現した。systemジョブにも必須SECRET_KEYの設定がなかったため、全ジョブ共通で空ENV_FILEとローカル検証用APP_ENV/SECRET_KEYを明示する。実環境のキーや設定を使う変更ではない。
