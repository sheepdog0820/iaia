# 実署名付き購入完了通知を解約後に処理する試験

2026-09-19、候補 `99acd8ca4d319c2579f74e4c1a102815ccd4a668` を隔離SQLite・一時HTTPサーバーと承認済みStripeサンドボックスで検証した。[結果JSON](STRIPE_CHECKOUT_SIGNED_ORDER_2026-09-19.json)の10項目すべて成功。

## 操作と結果

1. テスト専用Customerと月額480円のCheckoutを作成し、実課金モードでないことを確認した。
2. ブラウザーのStripe hosted Checkoutを公式テストカードで完了した。Stripe CLIから転送される実際の `checkout.session.completed` の本文・元の署名をメモリー内で保留し、HTTP 200のみ返した。
3. 購読作成通知でローカル権限が有効になった後、この試験で作成したSubscriptionをサンドボックスで解約した。削除通知によりローカルの購読・権限が解約済み・無料になった。
4. 未処理の購入完了通知を元の本文・署名で処理した。署名検証に成功し、最新の解約状態が維持され、プレミアム権限は復活しなかった。
5. 受信した購入完了・購読作成・購読削除・請求成功の4通知は、結果採取時点ですべて処理成功だった。

これは実際の購入完了通知の遅延順序を再現する試験であり、同時実行のすべての組み合わせを検証したものではない。[単体・PostgreSQL同時処理試験](STRIPE_CHECKOUT_ORDERING_2026-09-19.md)を補完する。

## 後片付け・未確認範囲

テスト購読は解約済み。一時サーバーと専用Stripe CLI listenerを終了し、隔離DBを破棄した。検証用ブラウザータブの残存もない。実課金、実利用者へのメール送信、main/AWS反映、Secrets・価格・税設定の変更は行っていない。公開証跡にはAPIキー、Webhook署名、通知の生本文、カード情報を含めない。

購入後のブラウザーはアプリに未ログインで、ログイン画面へ移動した。ログイン状態での購入戻り画面は未確認。一時WSGIサーバーは静的ファイルを配信せず、CSS等の404があったため、画面表示品質の合格証跡とはしない。新旧の複数契約を混在させる実署名通知、その他の並行競合、AWS接続・メール配送は残る。

ソース候補のCI全6ジョブ成功は[候補検証](STRIPE_CANDIDATE_CONTAINER_2026-09-19.md)を参照。この証跡追加は文書のみであり、正式公開判定は引き続きNo-Go。
