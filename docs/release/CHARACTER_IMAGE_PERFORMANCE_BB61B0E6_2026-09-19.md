# 最新候補の画像操作性能（増強案の隔離再測定）

2026-09-19、通常配布イメージ `tableno:stripe-candidate-bb61b0e6`、ID `sha256:e2d81b173fd6e7b632c0c2990e198559d6a669442bce85e6c7e4530961616fd3` で、[既存の1 CPU/2 GiB試験](CHARACTER_IMAGE_PERFORMANCE_2026-09-12.md)を再実行した。画像追加・一覧・他者拒否・削除・空一覧確認の計500要求が期待結果に一致し、予期しないエラー0。

登録100人、同時10フロー、6版/7版各50人、2,048×3,072・4,553,465 bytesの合成JPEG。非公開グループ100、キャラクター/シナリオ/セッション各1,000を新規DBに作成。Token認証のHTTP要求開始から本文取得までを測り、各操作100件のnearest-rank p95を算出した。画像生成・起動・種データ作成は測定外。

| 操作 | p95秒 |
| --- | ---: |
| 画像追加 | 1.776 |
| 画像一覧 | 0.714 |
| 他者一覧拒否 | 0.564 |
| 画像削除 | 0.679 |
| 空一覧確認 | 0.631 |

[集計JSON](CHARACTER_IMAGE_PERFORMANCE_BB61B0E6_2026-09-19.json)を保存した。全要求の結果と再現スクリプトはGit管理外の `C:/Users/endke/Workspace/iaia/tmp/large-image-onecpu-bb61b0e6/`。画像追加は201・順序0・メイン指定・URL存在、一覧は保存IDのみ、他者404、削除204・空本文・再取得0件を確認。ダウンロードした画像の画素一致は測定していない。

専用internalネットワーク、PostgreSQL 18.3（2 CPU/1 GiB）、Redis 7（128 MiB）、ローカルファイル保存を使用。アプリソースの差し替えなし。ラベルで所有を確認し、専用コンテナ・ネットワークの削除と不在を確認した。実AWS・S3・実ユーザー・実メールは使用していない。

本測定は増強案の1 CPU/2 GiBに対する単発検査で、現在のAWSの0.25 CPU/512 MiBでの合格を証明しない。cc160e7bから今回候補への確認対象（画像ビュー・シリアライザー・キャラクターモデル・entrypoint）に差分は見当たらなかったが、過去結果だけで新候補の性能を推定せず実測した。機能・画質・サイズ上限・権限条件は変更していない。

AWS容量増強は継続費用の承認対象であり未実施。今回の既存Webサイズを維持する[アプリ反映案](STRIPE_AWS_APP_APPROVAL_2026-09-19.md)にも含めない。現行容量、実S3/ブラウザ、同一キャラクター競合、5枚上限付近、長時間負荷、主要操作全体の確認は残り、Q02と正式公開No-Goを維持する。

## 現行容量との比較

同じ候補・同じ4,553,465 bytesの画像・登録100人/同時10フローを、アプリ0.25 CPU/512 MiBで新規の隔離DBから測定した。その他のDB/Redis構成・データ・判定条件は同じで、500要求すべて期待結果に一致、予期しないエラー0。[集計JSON](CHARACTER_IMAGE_CURRENT_CAPACITY_2026-09-19.json)を保存した。

| 操作 | 現行相当0.25 CPU/512 MiBのp95秒 | 増強案1 CPU/2 GiBのp95秒 |
| --- | ---: | ---: |
| 画像追加 | 6.957 | 1.776 |
| 画像一覧 | 2.996 | 0.714 |
| 他者一覧拒否 | 2.202 | 0.564 |
| 画像削除 | 3.094 | 0.679 |
| 空一覧確認 | 2.512 | 0.631 |

現行相当の容量は画像追加・削除が3秒基準に未達。一覧も閾値に近く、余裕があるとは扱わない。測定中のdocker stats単発観測ではCPU25.05%、メモリ478.3 MiB/512 MiBだった。ピーク値・スロットリング全量・原因の確定には使用しない。CPUとメモリを同時に変えた単発比較であり、CPUだけに起因するとの結論は出さない。

証跡とスクリプトはGit管理外の `C:/Users/endke/Workspace/iaia/tmp/large-image-current-bb61b0e6/`。所有ラベルを照合し、試験資源の削除と不在を確認した。これで上記の「現行容量」は限定条件の未確認から実測未達に変わる。実AWS/S3・全フローの検証は残る。Web容量増強の実行や継続費用の承認を得たとは扱わない。
