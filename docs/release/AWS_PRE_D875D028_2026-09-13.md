# 開発AWSへの確認済み版反映

## 対象と承認

2026-09-13の「確認完了しているものをmainにマージ」「開発AWS環境に移行」の指示により、確認済みコミット `d875d028ede0b3172780d54d4baacdb226a1d3b3` をmainへfast-forwardし、既存の `tableno-aws-pre` サービスへ反映した。作業中の添付操作画面は含まない。

## 配布物と事前検証

- ECRタグ: `aws-pre-d875d028`
- 配信digest: `sha256:59542e45e5dc8e1e33cbf7202eb12911ffbb390156a778fe7eb11f0fe4c65f79`
- ローカル通常イメージID: `sha256:7124efbe297e870f033e1721a1590963c2e6e394227ba65d0a332ff7aa960936`
- git archiveとイメージ内の507ソースファイルが一致。関連39件と保存済みURLのGM権限取り消し1件のテスト成功。
- [PR CI](https://github.com/sheepdog0820/iaia/actions/runs/34700105294)・[push CI](https://github.com/sheepdog0820/iaia/actions/runs/34700102559)とも全6ジョブ成功。単体・統合は各1,832成功・30スキップ・159警告、ブラウザは各186成功。PR PostgreSQLは293成功・48サブテスト成功。

## 実施内容

タスク定義48（f97c7809）から49へ更新し、イメージをdigestで固定。CPU256・メモリ512MiB、IAM、Secrets参照、ネットワーク、ログ設定は維持した。

個人事業者の請求開示方式、support@tableno.jp、月額480円・年額4,800円（税込）、承認済みの契約・解約・返金案内を公開用環境変数へ反映。`STRIPE_CHECKOUT_ENABLED=False` と `LEGAL_DISCLOSURE_OPERATIONS_READY=False` を維持する。開示体制未確認のまま販売を有効にすると起動検査で拒否されることを通常イメージの本番設定で検証した。

DBスキーマと静的ファイルの差分なし。変更テンプレートも新しい静的ファイルを参照しないため、migrate・collectstatic・CloudFront invalidationは実施していない。起動時のmigrate/collectstatic/開発ユーザー作成もFalseを明示した。常設リソース追加・CPU増強・継続費用増加・実課金は行っていない。

WindowsのAWS CLIで日本語JSONの読込と応答デコードに失敗した。登録結果をAWSから再取得し、既に登録された49が候補と一致することを確認して利用した。環境変数の配列順序は名前で正規化して照合。重複登録・文字化けした設定の反映は行っていない。

## 反映確認

2026-09-13 12:17 JST時点でサービスは定義49だけの構成でrollout COMPLETED、desired/running=1、pending=0。稼働タスク `39273a8184da4b8bacd4d04b7a5d8591` はRUNNING/HEALTHYで、上記digestと一致する。

`https://stg.tableno.jp/health/ready` はstatus/database/cacheともok（03:17:27 UTC）。新タスクのCloudWatchログをERROR/Traceback/CRITICALで検索し、確認時点で該当0件。

指定Chromeの既存ログインで特商法ページとプレミアム管理へ遷移し、請求開示方式・日本語の料金条件・Checkout停止表示を確認。特商法ページは画面も目視確認し、メールリンクを読み取れる。取得したブラウザerrorログは0件。メール・決済・Google等への送信はしていない。

ログイン後のセッション一覧へメニューから移動し、統計と一覧の読み込み完了を確認。反映記録・READMEの更新に対する文書テスト39件も成功した。

## 限界と復旧

今回の反映確認は実Stripeの課金ライフサイクル、Google Calendar/Sheetsの実同期、変更後の全権限パターンをAWS実データで試すものではない。正式公開は引き続きNo-Go。添付操作画面は作業ブランチで検証中で、今回の配布物に含まれない。

インフラ上の直前復旧先は `tableno-aws-pre:48`。DB移行や共有静的ファイルの置換を行っていないため、アプリ切り戻しで以前の構成に戻せる。ただし48は修正前のハンドアウト書き込み権限不備を再導入する。安易な恒久復帰先として扱わず、障害時は影響する操作の制限と、修正を保持した再配布を優先して判断する。
